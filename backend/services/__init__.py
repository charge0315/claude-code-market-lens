"""ドメインロジック層.

Market Lens `backend/services/` を再利用元とし、フェーズごとに移植する。
サブパッケージ構成は `plans/02_アーキテクチャ.md` §2 を参照:
data / vault / scoring / picks / ledger / learning / registry / inference / portfolio / notify / db。
"""
