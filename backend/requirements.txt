# Alpha Forge backend 依存（P1 基盤）。
# ML ライブラリ（xgboost / scikit-learn / torch）は P3/P5 で移植時に追加する。
# バージョン方針は Market Lens に揃える（同一構成踏襲）。

fastapi==0.141.1
uvicorn[standard]==0.39.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.2

# DB / マイグレーション
SQLAlchemy[asyncio]==2.0.44
aiosqlite==0.21.0
alembic==1.19.1

# 非同期基盤
celery>=5.6.3
redis>=8.1.0

# LLM（P3、🆕 マルチLLM対応でOpenAI追加）
anthropic==1.2.0
openai==2.7.0

# HTTP クライアント（外部 API 取得層で使用）
httpx==0.28.1

# データ層（P2）。バージョンは Market Lens に揃える。
pandas==3.0.5
yfinance==1.7.0
PyYAML==6.0.3

# pandas の推移依存。未固定だと CI（Python 3.13 マトリクス）で pip がより新しい numpy を
# 解決することがあり、その型スタブが PEP 695 の `type` 文（Python 3.12+ 構文）を使うため
# mypy の python_version=3.11（pyproject.toml）設定と衝突して構文エラーになる。全環境で
# 同一バージョンに固定して解消する。
numpy==2.4.6

# 評価指標（P4）: Brier / ROC-AUC / isotonic 較正。
scikit-learn==1.7.2
joblib==1.6.0

# ML predictor（P5d）: 断面プール分類器。xgboost>=3.3.0 は Python 3.11 サポート打ち切りのため上限固定
# （Market Lens requirements.txt と同じ理由）。
xgboost>=3.2.0,<3.3.0

# 深層学習（P9、銘柄別 LSTM/Transformer）。CPU版インストール:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# CI（requirements-ci.txt）には含めない。重く、torch 依存テストは import できない環境でも
# 収集エラーにならないよう `TORCH_AVAILABLE` ガード + `pytest.importorskip("torch")` で
# スキップする（Market Lens と同じ方針）。
torch>=2.13.0

# Web Push（VAPID）
pywebpush==2.0.3

# note下書き用の表画像（🆕）: note.comはSVGアップロードを受け付けないため、JPEGでラスタライズする
# （`services/notes/table_image.py`）。Windowsシステムフォント（Noto Sans JP等）で日本語を描画する。
Pillow==12.3.0
