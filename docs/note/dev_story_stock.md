# ALPHA FORGE 開発秘話 ネタストック（連載管理）

note.com 連載「ALPHA FORGE 開発秘話」の**ネタ在庫と進行管理**。1回＝スマホで5分前後（本文2,500〜3,500字）で
読める分量に切り出し、1日1話ずつ小出しに公開する。デイリーAIピック記事（`YYYY-MM-DD/note.txt`）とは
ファイル名を分け、各話の本文は `docs/note/YYYY-MM-DD/dev_note_epNN.txt` に置く。

ネタの出典は Claude Code との過去の会話履歴（2026-09-10〜）、`plans/05_決定ログと未決事項.md`、
`git log` のコミット本文、`docs/operations.md`。数字・日付は出典で確認済みのものだけを使う。

## 連載の3本柱（2026-09-23 ユーザー要望で追加）

1. **事件簿**: 実際に起きた障害・失敗と、その原因と対処（連載の主軸）
2. **プロンプトエンジニアリング**: AI（Claude Code）への指示の書き方と、アプリ内LLMへのプロンプト設計
3. **ハーネスエンジニアリング**: Claude Code を「使いこなす仕組み」側の設計（CLAUDE.md・スキル・メモリ・フック・/loop・
   サブエージェント・計画モード・ローカルCI）

柱2・3は専門回（下表の【P】【H】）で深掘りしつつ、事件簿回でも末尾に「🧰 今回のプロンプト／ハーネス術」を
1コーナー（300〜500字）置き、その事件から生まれた指示文・ルール・スキルを紹介する。事件→仕組み化の流れを毎回見せる。

## 執筆ルール（この連載固有）

- 書式・文体・コンプライアンスは `NOTE.md`（系統B）と `docs/note/templates/dev_note_template.txt` に従う。
  ただし連載1話ぶんに収めるため、テンプレの7章をそのまま使わず「導入＋3〜4大見出し＋次回予告＋CTA＋免責」に圧縮する。
- 大見出しは絵文字始まり（note.com エディタの `N. ` 自動番号リスト化を避ける。`note-com-draft` スキル参照）。
- **公開NG**: ローカルパス、ユーザー名、APIキー名の実値、メールアドレス、前身ツールの固有名（「前身の自作ツール」と書く）、
  推奨買値/損切値/売値の具体値、個別銘柄の売買推奨と取れる表現。
- プロンプト・CLAUDE.md・スキルを引用するときは要旨か短い抜粋にとどめ、パス・キー・環境固有値はダミーに置き換える。
- 失敗は失敗として書く。盛らない（実測値が無いものは数字を出さない）。
- 各話の末尾で次回予告を1行入れる。公開後は下の進行表の「状態」「公開日」を更新する。
- ハッシュタグ（毎話共通の核）: `#ALPHAFORGE開発秘話 #個人開発 #ClaudeCode #AIエージェント #生成AI #機械学習 #日本株`
  ＋回ごとに2〜3個（【P】回は `#プロンプトエンジニアリング`、【H】回は `#ハーネスエンジニアリング` `#CLAUDEmd` 等）。
  `#株式投資` `#投資` など売買を連想させるタグは付けない。

## 進行表

区分: 【事】事件簿 / 【P】プロンプトエンジニアリング / 【H】ハーネスエンジニアリング

| # | 区分 | 状態 | 公開予定/公開日 | タイトル案 | 本文ファイル |
|:--|:--|:--|:--|:--|:--|
| 01 | 【事】 | 下書き保存済 | 2026-09-23 | 構築プロンプト1枚から、2日で「育つAI端末」が立ち上がるまで | `2026-09-23/dev_note_ep01.txt` |
| 02 | 【事】 | ストック | — | 初めての朝、AIピックが1銘柄のせいで全滅した | — |
| 03 | 【P】 | ストック | — | 「作って」ではなく「約束」を書く ― 構築プロンプトの設計図 | — |
| 04 | 【事】 | ストック | — | 寄り付きに間に合わない！ 23分半かかったピック生成 | — |
| 05 | 【H】 | ストック | — | CLAUDE.md はプロジェクトの憲法 ― 事件が起きるたびに条文が増えた | — |
| 06 | 【事】 | ストック | — | 日曜日に勝手に働き出したAI ― 休場日ガード顛末記 | — |
| 07 | 【事】 | ストック | — | ニュースが全銘柄0件だった日 ― APIの静かな仕様変更 | — |
| 08 | 【P】 | ストック | — | LLMに「3つの価格」を言わせる ― 強制ツール呼び出しとサーバ側検証 | — |
| 09 | 【事】【P】 | ストック | — | 自作AIのプロンプトインジェクションの抜け穴を、自分で見つけた話 | — |
| 10 | 【事】 | ストック | — | 「未来のカンニング」を構造で防ぐ ― ポイント・イン・タイム台帳 | — |
| 11 | 【事】 | ストック | — | 四季報の自動取得を“辞退”した日 | — |
| 12 | 【事】【P】 | ストック | — | ローカルLLMで全銘柄チャート読解はできるのか？ 実験と撤退 | — |
| 13 | 【事】 | ストック | — | note.com という最後の壁 ― SVG・表・番号リストとの格闘 | — |
| 14 | 【H】 | ストック | — | 失敗を「スキル」と「メモリ」に変える ― 同じ罠に二度はまらない仕組み | — |
| 15 | 【事】 | ストック | — | 学習プロセスが子プロセスで即死する ― Windows × spawn の罠 | — |
| 16 | 【事】 | ストック | — | 600秒無応答 → 103秒 ― 捨てるための計算を止めた話 | — |
| 17 | 【事】 | ストック | — | 黒からクリーム色へ ― 配色を3回ひっくり返したUIの話 | — |
| 18 | 【事】 | ストック | — | 台帳を汚さない「試し打ち」モード ― 確度38%で却下された7203 | — |
| 19 | 【H】 | ストック | — | AIに任せる範囲、止める仕組み ― 計画モード・フック・/loop・課金前確認 | — |
| 20 | 【事】 | ストック | — | タイムマシンで学習する ― 過去日リプレイ学習と予算15ドル | — |
| 21 | 【事】【H】 | ストック | — | ドキュメントは嘘をつく ― 実装と乖離したMDの大掃除 | — |
| 22 | 【事】 | ストック | — | 「毎日100円で売れないか」― マネタイズ構想と3値を出さない決断 | — |

---

## 各話ネタ詳細

### #01 構築プロンプト1枚から、2日で「育つAI端末」が立ち上がるまで（プロローグ）【事】

- 出発点: 前身の自作株分析ツール（毎朝の市況レポート中心）。「当てた/外れた」を記録して賢くなる仕組みが無かった。
- 2026-09-10: 構築プロンプト（Markdown 1枚）を渡して「本フォルダで構築してください」と一言。
- 設計の柱（プロンプトに書いた約束）: ①全予測を台帳化し自分の実測値だけで学習（継続学習ループ）
  ②AIが売買銘柄を出すなら推奨買値/損切値/売値の3値を必須化しサーバ側で整合性検証
  ③モデル入れ替えは提案のみ・人間承認制 ④ブローカー発注を一切しない ⑤上げ=赤/下げ=緑（国内証券標準）
  ⑥外部テキストをLLMプロンプトへ直接入れない。
- 2026-09-11 06:43 計画ドキュメント → 2026-09-12 07:28「P0〜P8全フェーズ完了」まで約25時間・27コミット。
- 人間側の入力は「次」「続行」「プッシュしました」がほとんど。途中で利用上限に到達し、リセット後に再開。
- 出典: git log（09-11〜09-12）、`plans/05` 変更履歴 2026-09-10/12、会話ログ。

### #02 初めての朝、AIピックが1銘柄のせいで全滅した【事】

- 2026-09-13 夜〜09-14: 「ピック生成に失敗しました」「設定画面がでないのもそのせい？」。
- J-Quants の無料プランでは必要データが取れず、その場でプランを更新。
- 根本原因（コミット `fix: ピック生成のブロッキングI/Oと1銘柄失敗時の全滅を修正`）:
  yfinance の同期呼び出しを async 関数内で直接実行 → 候補銘柄ごとにイベントループ全体が停止し
  バックエンド全体が応答不能。さらに1銘柄の取得失敗（サーキットブレーカOPEN等）が500として全体を落としていた。
  → `asyncio.to_thread` で逃がす＋銘柄単位で try/except スキップ。
- 同日、ナレッジベース検索API（別サービス）が未起動 → 全銘柄で30秒タイムアウトを繰り返しピック生成が大幅遅延。
- 🧰 コーナー: 「AIピックに失敗しています。根本原因をさぐってください」― 症状でなく**根本原因**を求める一言の効き目。
  そして「システム起動して」と言われたら依存サービスまで全部確認する、というルールが CLAUDE.md に入った（→ #05）。
- 出典: 会話ログ 09-13〜14、コミット本文、`docs/operations.md` §4.5。

### #03 「作って」ではなく「約束」を書く ― 構築プロンプトの設計図【P】

- 題材: `新規プロジェクト構築プロンプト.md` の章立て（公開時は要旨のみ・パス等はダミー）。
  0 エレベーターピッチ → 1 スコープ（In/**Out of Scope＝明示的に作らないもの**）→ 2 継続学習ループを「最優先で設計」
  → 4 データソース仕様 → 6 スコアリング → 9 日本株ドメインルール（厳守）→ 11 決定事項と**残る未決事項**
  → 13 開発フェーズ計画（各フェーズ完了時にコミット＆CI全通し）→ 14 **受け入れ基準（Definition of Done）** → 15 Claude Code への作業指示。
- ポイント:
  - 機能一覧より「破ってはいけない約束」と「作らないもの」を先に書く（AIは頼まれていない機能まで作りがち）。
  - 未決事項を書いておくと、AIが勝手に決めずに計画フェーズで質問してくる（実際に P0 で3件確認された）。
  - 受け入れ基準があると「完了」の判定がブレない。
  - 「移植を第一選択」と書くことで、ゼロから書き直す暴走を防いだ。
  - **プロンプトは育てる**: 運用で得た知見（マルチLLM、PIT、note生成、並列学習、beat冪等化）を後から §12 に還流し、
    次に同種のシステムを作るときの原本にしている。重複していた別の構築プロンプト集は統合して削除（09-22）。
- 出典: `新規プロジェクト構築プロンプト.md`、`plans/05` 2026-09-10/22。

### #04 寄り付きに間に合わない！ 23分半かかったピック生成【事】

- 2026-09-16 朝:「ピックに時間がかかっていて、市場がひらくのに間に合わないね」。
- 08:50 JST 開始で実測23分半（中長期のみ）。Windows の celery worker は `--pool=solo`（直列）。
- → 07:40 開始へ前倒し（09-17）、note下書き連携のためさらに 07:30 へ。
- 「間に合わせる」は性能改善より先にスケジュールで解く、という判断。
- 🧰 コーナー: 実測値を根拠にコミットメッセージへ残す（「実測23分半」）→ 後で判断を見直すときの材料になる。
- 出典: コミット `fix: AI銘柄ピック生成の開始時刻を寄り付き前に間に合うよう前倒し`、`plans/05` 2026-09-17。

### #05 CLAUDE.md はプロジェクトの憲法 ― 事件が起きるたびに条文が増えた【H】

- CLAUDE.md = Claude Code が毎セッション最初に読む「プロジェクト固有ルール」。一般論は書かず、このプロジェクトでしか
  通用しない約束だけを書く方針（冒頭に明記）。
- 条文の例（公開時は要旨）:
  - ドメイン: 時刻はJST・大引け15:00、上げ赤/下げ緑をトークン経由で、銘柄コードは4桁で `.T` を付けない。
  - 安全: 外部テキストをプロンプトへ入れない境界、ブローカー発注を一切しない、モデル昇格は提案のみ。
  - Git: 同じ作業ツリーを人間も並行編集するので **`git add -A` 禁止**（対象ファイルを明示 add）、フェーズごとにコミット。
  - 検証: push 前にローカルCI相当（ruff/black/mypy/bandit/pytest、tsc/jest/eslint/next build）を全通し。
- **口癖をマクロにする**: 「システム起動して」＝ frontend/backend/Redis/worker/beat/ベクトル検索API を確認し、止まっているものだけ起動。
  「Note作成」＝ 下書き生成→書き出し→note.com へ下書き保存まで（公開は押さない）。
- **事件→条文**の年表: 09-14 ナレッジ検索API未起動で遅延 → 起動チェックリスト追加 / 09-14 beat 一斉発火 → beat を自動起動対象外に
  → 09-22 根本原因解消を確認して起動対象へ戻す（条文は削除・改正もする）/ 09-17「公開まで一気に」→「下書きまで」に確定 /
  09-18 インジェクション抜け穴 → 境界条文にトレンド分析経路を追記。
- グローバル設定（全プロジェクト共通の CLAUDE.md とルール群）との二層構造: 日本語で会話・コミット、可逆な作業は確認なしで自走、
  コミット/push/公開/削除は確認必須。
- 出典: `CLAUDE.md`、`git log -- CLAUDE.md`、`plans/05` 2026-09-22。

### #06 日曜日に勝手に働き出したAI ― 休場日ガード顛末記【事】

- 2026-09-14: celery-beat 初回起動で溜まっていたスケジュールが一斉発火 → ピック二重生成・学習バッチ暴走。
  一時的に beat を自動起動対象から外す運用に。
- 2026-09-20（日曜）: beat の「追いつき起動」で休場日に金曜終値ベースのピックが生成された（実害: 無効ピック）。
  ユーザー「今日はマーケット開いていないです」「次の市場が開くのは24日ですね」（シルバーウィーク）。
- 対策: 平日判定 → `jpholiday` で祝日も除外 → 当日分の既発行チェック（冪等化）。テストで固定。
- 09-22 に根本原因解消を再点検し、beat を起動対象へ戻した。週次タスクは重複防止ガードなしだが「提案書き込みのみ」のため許容。
- 🧰 コーナー: 再発防止は「気をつける」ではなくテスト（`test_run_picks_task_idempotency.py`）で固定する。
- 出典: `plans/05` 2026-09-22、コミット 09-20 15:21/16:14/16:38、CLAUDE.md。

### #07 ニュースが全銘柄0件だった日 ― APIの静かな仕様変更【事】

- 2026-09-18: yfinance のニュース応答が `title` 等をアイテム直下から `content` 配下へ移しており、
  既存ロジックは常に空文字 → 全銘柄「ニュース0件」扱い。エラーにならないので気付けなかった。
- 新旧両形式を吸収するヘルパーで修正。同日、ニュース見出しのLLMセンチメント判定をピックに反映。
- 教訓: 「失敗しない失敗（silent failure）」が一番怖い。
- 出典: コミット `fix: yfinanceニュース応答のフィールドネスト変更にセンチメント取得を追従`。

### #08 LLMに「3つの価格」を言わせる ― 強制ツール呼び出しとサーバ側検証【P】

- 題材: `backend/services/picks/prompt.py`（`build_pick_prompt`）。
- 設計:
  - LLMに自由に語らせず、**強制ツール呼び出し（forced tool-use）`propose_stock_pick`** で
    推奨買値・損切値・売値・確信度・根拠を**型の決まった形**で返させる。
  - 材料は「自分の定量分析結果」: 合成スコア、方向一致度、サブスコア内訳、テクニカル/ファンダシグナル、
    ATRベースの目安ブラケット。LLMはゼロから考えるのではなく**定量分析の上に解釈を足す**役割。
  - 外部由来の情報は frontmatter・enum・数値だけを「外部由来」と明記して載せる（→ #09）。
  - 返ってきた3値はサーバ側で `損切 < 現在値 < 売値` かつ `損切 < 買値 < 売値` を検証し、不整合は却下か目安ブラケットで補完。
  - 確度は生値のまま出さず、isotonic/Platt で過去の実測勝率に合わせて較正してからUIへ。
- 後日の発見（09-22）: 根拠は2〜4文・1024トークン上限、拡張思考なしの設計で、「深掘りログを全部見たい」と頼んだら
  **そもそも長い思考はシステム内に存在しない**と判明。プロンプト全文も保存していなかった → 何を残すかも設計のうち。
- 出典: `backend/services/picks/prompt.py`、CLAUDE.md「スコアリング & 3値」、`plans/05` 2026-09-22。

### #09 自作AIのプロンプトインジェクションの抜け穴を、自分で見つけた話【事】【P】

- 原則: 外部由来テキスト（ニュース本文・ノート本文）はLLMプロンプトへ入れない。見出しを読むのは隔離LLMだけ、
  本体へは数値/enumのみ転送、隔離LLMの自由記述 `reasoning` は他LLMへ転送しない（**間接インジェクションのロンダリング防止**）。
- 2026-09-18 ユーザー依頼「外から来た文章をプロンプトに入れないことが守られているか検証して」で全ソース棚卸し。
  トレンド分析経路だけ抜けを発見: 生ニュース見出しを読んだLLMの自由記述（テーマ名・要約・相関理由）が本体プロンプトへそのまま流れていた。
- 数値・enum・検証済み銘柄コードのみに絞り、自由記述が流れないことをテストで固定。UI表示用には残す。
- プロンプト設計の型:「プロンプトに載せてよいものリスト」（frontmatter / enum / number / 検証済みコード）を先に決め、
  境界モジュールを1か所に集約してテストで固定する。ベクトル検索も「発見」にだけ使い本文は返さない。
- 🧰 コーナー: 「守られているか検証して」という**監査型の依頼**は、実装依頼と同じくらい価値がある。
- 出典: `plans/05` 2026-09-18、CLAUDE.md、`backend/services/picks/prompt.py` docstring。

### #10 「未来のカンニング」を構造で防ぐ ― ポイント・イン・タイム台帳【事】

- 2026-09-18 ギャップ分析: MLの特徴量がテクニカルのみ／Vaultの銘柄ノートは上書き更新なので「当時の値」を復元できない
  （lookahead bias）。
- → `pit_*_snapshots` を新設し `merge_asof(direction="backward")` で未来スナップショット混入を構造的に不可能に。
  被覆率ゲートは推奨（60営業日/50%）より厳しく 120営業日/70% に。ソースアブレーション評価を初実装。
- 🧰 コーナー: 「実装との突き合わせを行い、MDを更新して、実装プランを考えて」― 作る前にギャップ分析させる頼み方。
- 出典: `plans/05` 2026-09-18（P29）、会話ログ 09-18。

### #11 四季報の自動取得を“辞退”した日【事】

- 2026-09-18 ユーザー「四季報のサイトからヘッドラインをとってきて」。
- 利用規約を確認 → 会員規約でコンピューター等による自動取得を禁止行為として明記、違約金条項（最低360万円）あり。
  robots.txt は全許可でも会員規約が優先 → 実装を辞退、調達は保留。今後も同サイトの自動収集は提案しない方針を明文化。
- 教訓: 「技術的にできる」と「やってよい」は別。AIが断ってくれたこと自体がハーネスの成果でもある。
- 出典: `plans/05` §3 #A、2026-09-18。

### #12 ローカルLLMで全銘柄チャート読解はできるのか？ 実験と撤退【事】【P】

- 2026-09-18: 全銘柄をLLMでチャート解析したらAPI料金はいくら？ → 現実的でない。
- ローカルの小型マルチモーダルモデルで試験（AIにプロンプトとチャート画像を作らせ、LM Studio で実験）。
  出力はどれも「sideways / neutral / confidence 40」、支持線・抵抗線は null。
- プロンプト側の工夫として、判断ではなく**観察だけ**を聞く切り分けテスト（陽線の本数、高値・安値の位置、全体の形）も実施したが、
  実用精度に届かず。「読めていないのか、判断できないのか」を切り分ける問い方の話として書ける。
- 結論: 「今の設定（値動き上位からの候補プール）のままでいく」。撤退も立派な意思決定。
- 出典: 会話ログ 09-18 05:18〜05:48（UTC）。

### #13 note.com という最後の壁 ― SVG・表・番号リストとの格闘【事】

- 2026-09-17: note 下書き自動生成。note.com は Markdown 表を解釈しない → 表を画像化。
  最初は SVG → 「SVGでは貼れないようです」→ Pillow で JPEG 化（日本語フォントのフォールバック込み）。
- 証券コード `^5243` 形式で記事内に株価チャートを埋め込める仕様を発見し活用。
- 「公開まで一気にやります」→ 2分後「やっぱり下書き保存までにしてください」。以後は下書きで止める運用に確定。
- 2026-09-22: エディタが空段落の `2. ` を番号リストに変換し数字を食べる罠。見出しは絵文字始まりへ。
- 2026-09-23（本連載第1話の投稿時）: 見出しは `## `、区切り線は `---` のMarkdownショートカットが効く一方、
  画像キャプション欄でEnterを押しても抜けられず、後続の本文がキャプションに吸い込まれた。
- 出典: コミット 09-17 群、`NOTE.md`「過去の学び」、`note-com-draft` スキル。

### #14 失敗を「スキル」と「メモリ」に変える ― 同じ罠に二度はまらない仕組み【H】

- **スキル**: 手順書をAIが呼び出せる形で保存したもの。2026-09-22「ここまでをnote.comへの下書き保存のSKILLとしてMD化して」
  → `note-com-draft` スキル誕生。中身は成功手順より**罠の記録**が主役:
  「公開ボタンは絶対に押さない」「`2. ` が番号リストに化ける→回避と復旧手順」「推奨価格はスクショでもマスク」
  「ファイル入力は直接クリックしない（見えないOSダイアログが開く）」「保存確認は別タブで」。
  その後ユーザーが大見出し7章・区切り線・銘柄小見出しのルールを追記して育てた。
- **メモリ**: 会話をまたいで残る「好み・方針・経緯」。例: 課金を伴う実機検証は計画承認後でも実行前に一度確認する。
  正直なエピソード: 「アプリ名を公開物に出さない」（09-22）→ 翌日には「ブランドとして前面に出す」へ方針反転。
  メモリは古くなる前提で、矛盾したら書き換える（本連載開始時に更新）。
- 使い分け: 「毎回同じ手順」はスキル、「このユーザー・このプロジェクトの判断基準」はメモリ、「コードで守れる約束」はテスト、
  「全員が毎回読むべき約束」は CLAUDE.md。
- 出典: `note-com-draft` SKILL.md、会話ログ 09-22、memory。

### #15 学習プロセスが子プロセスで即死する ― Windows × spawn の罠【事】

- 2026-09-20: 学習を `ProcessPoolExecutor` で並列化。Windows の spawn は子プロセスを新規インタプリタで起動するため
  ①テストの monkeypatch（モデル保存先隔離）が子へ引き継がれない ②celery.exe 配下では `sys.path` が引き継がれず
  `ModuleNotFoundError: No module named 'backend'` で即死（uvicorn 経由では起きない＝起動方式依存）。
  → プール生成を差し替え可能な間接層に／PYTHONPATH を明示。
- 同日「XGBoost と RandomForest のカバレッジが95%止まりなのはなぜ」→ クラッシュ前のデータを整理し「完全初期化して再学習」へ。
- 出典: `plans/05` 2026-09-20、コミット 09-20 16:14。

### #16 600秒無応答 → 103秒 ― 捨てるための計算を止めた話【事】

- 2026-09-21: プールMLモデル未登録時でも東証全銘柄の価格を逐次取得 → 結果は結局捨てられていた（完全な無駄）。
- さらに yfinance 側の timeout=30 が Windows 実機で効かず、1銘柄が90秒超ハング → 全体が応答不能。
- 未登録時はスキャンをスキップ＋アプリ側で1銘柄20秒の打ち切り。600秒無応答 → 約103秒で正常完了。
- 出典: コミット `fix: ピック生成がユニバース全銘柄の無駄な逐次スキャンで応答不能になる不具合を修正`。

### #17 黒からクリーム色へ ― 配色を3回ひっくり返したUIの話【事】

- 当初はダーク系（near-black＋ブルー）→ 09-13 に明るいクリーム色 "Organic"（`#f5ead8`＋オレンジ `#c67139`）へ全画面統一
  （配色テーマ2回全面切替）→ 09-20 に Claude Design で作ったモックアップを全画面適用（Caprasimo/Figtree フォント）。
- 上げ赤/下げ緑はブランド配色と独立したドメイン規約として死守。生 `<table>` 禁止・ブレークポイント4種をテストで強制。
- 🧰 コーナー: デザインは言葉で説明せず、HTMLモックを渡して「このスタイルを全画面で踏襲して」。
- 出典: `plans/05` 2026-09-13/20/22、会話ログ 09-12/09-20。

### #18 台帳を汚さない「試し打ち」モード ― 確度38%で却下された7203【事】

- 2026-09-22: チャート画面で任意銘柄を指定してAI推論の全過程をライブ表示したい。
- 推論オーケストレータは台帳へ書かない（確定は呼び出し元）ことを確認し、台帳を汚さない sandbox を新設。
  未使用だった日次LLMコスト上限（既定$5）を初めて実効化。
- 実機検証（課金前に確認）で 7203 を中長期指定 → 確度38%が採用基準40%未満で却下。台帳件数は前後で変化なし（76件）を DB で確認。
- 出典: `plans/05` 2026-09-22（P36）。

### #19 AIに任せる範囲、止める仕組み ― 計画モード・フック・/loop・課金前確認【H】

- 任せる範囲の線引き（グローバル指示）: コード編集・テスト・調査は確認なしで自走、コミット/push/公開/削除/課金は確認必須。
- **計画モード**: 複数ファイルにまたがる変更は「プランを提示して」→ 承認後に実装（候補プール可視化、試し打ちモード等）。
  重い設計はより強いモデルに考えさせる（「実装プランをOpusで考えてください」09-18）。
- **フック**: セッション開始時に前回の要約を読み込む、編集後の整形・検査など、AIの外側で毎回必ず走る処理。
- **/loop**: 長い学習ジョブの進捗を定期確認させる（09-20、4モデルの学習進捗監視）。
- **ワークツリー / サブエージェント**: 並行作業を分離し、終わったらマージして破棄。
- **ローカルCI全通し**: push 前に backend/frontend の検査を全部。CIが落ちたら「CI通して」で直させる。
- 失敗談: ハーネス仕様書（CLAUDE_HARNESS.md）を別に作ったら CLAUDE.md と二重管理になり、1日で内容が食い違って削除した（→ #21）。
- 出典: グローバル CLAUDE.md・rules、会話ログ、`plans/05` 2026-09-22。

### #20 タイムマシンで学習する ― 過去日リプレイ学習と予算15ドル【事】

- 2026-09-23:「取得できるもっとも古いデータからAIピックを行い、次の日のデータで答え合わせして学習を進めたい」。
- 第1段階は LLM なし（テクニカル＋ML）、第2段階（LLM あり）は予算上限 $15。未来リーク排除が最重要。
- 走らせた結果「ソースアブレーション評価は変わらない」「銘柄別モデルの品質分布も変化なし」― 効かなかった事実も記録する。
- 🧰 コーナー: 予算を数字で渡す（「第2段階の予算は15ドルまで」）と、AIの計画がその枠内で組まれる。
- ※公開前に最新の実測を確認して書く（進行中）。
- 出典: `plans/05` 2026-09-23、会話ログ 09-23。

### #21 ドキュメントは嘘をつく ― 実装と乖離したMDの大掃除【事】【H】

- 2026-09-22 横断監査:「祝日は非対応」と書かれたまま（実際は対応済み）、画面一覧が6つのまま（実際は7）、
  旧配色のデザインMD、どこからも参照されない重複仕様書が内部矛盾を起こしていた等。3ファイル削除。
- AI と作ると実装速度にドキュメントが追いつかない。「MDファイルと実装の差異がないか確認して」を定期的に頼む。
- ハーネス観点: AIが読む文書（CLAUDE.md・計画書）が古いと、AIは古い前提で正しく間違える。単一情報源を決めて重複を消す。
- 出典: `plans/05` 2026-09-22。

### #22 「毎日100円で売れないか」― マネタイズ構想と3値を出さない決断【事】

- 2026-09-17: 「このシステムをマネタイズしたい」→「AIピックの根拠をnote化して毎日100円で販売できないか」。
- 推奨価格（3値）を公開すると投資助言（金商法）に触れうる → 「3値は出さない方針」「半自動方式」で決定。
  以後、記事の価格は全マスク、理由も明記する運用に。
- 出典: 会話ログ 09-17、`NOTE.md` コンプライアンス節。

---

## 追加ネタ候補（未整理メモ）

- マルチLLM（Claude/Gemini/ChatGPT）併用と「公式/シャドウ」表記をモデル名へ変えた話（09-16〜17）【P】
- ポートフォリオ承認待ちチケットが5分ごとに積み上がる問題（09-18）
- 決算サプライズ軸・需給（信用残）軸の追加と J-Quants の制約（09-22）
- ナレッジベース（Obsidian＋ベクトル検索）を別AIエージェント向けの指示書MDで改修させ、レビューした話（09-14）【P】【H】
- note 記事の文体ルール（10原則・専門用語の言い換え表）をAIに渡して記事を書かせる話（`NOTE.md`）【P】
