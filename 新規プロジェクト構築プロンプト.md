# 新規プロジェクト構築プロンプト — 「AI 銘柄ピック & 継続学習トレーディング端末」（コードネーム: *Alpha Forge*）

> **このファイルの使い方**
> 新規リポジトリのルートでこのファイルを Claude Code に読み込ませ、「このプロンプトに従ってプロジェクトを立ち上げてください」と指示する。
> Claude Code は最初に **調査 → 計画提示 → フェーズ分割** を行い、`plans/` 配下に計画ドキュメント一式（PRD / アーキテクチャ / システム設計 / タスクリスト）を生成してから実装に入ること。
> 既存プロジェクト **Market Lens**（`C:\Users\charg\myWorkspace\market-lens`）は再利用元。ドメインロジック・データ取得層・スコアリングは**移植を第一選択**とし、ゼロから書き直さない。
> §1〜§11 が P0〜P8（初期構築）の本体プロンプト、§12 が運用フェーズ（P9以降）で得た発展的機能の追加プロンプト集（🔧2026-09-22、旧 `PROJECT_CREATION_PROMPT.md` を統合）、§13〜§15 がフェーズ計画・受け入れ基準・作業指示。

---

## 0. エレベーターピッチ

複数の独立した情報源（4分析・最新トレンド・毎日のニュース・四季報・Obsidian ナレッジ）を統合し、
**中長期銘柄**と**短期（デイトレ）銘柄**をそれぞれ「推奨買値 / 損切ライン / 推奨売値」の 3 値付きでピックアップする AI トレーディング支援端末。

差別化の核は 2 つ:

1. **継続学習ループ** — すべての予測を台帳化し、決着結果からモデルを再学習。予測精度の成長を指標化・可視化する。
2. **AI の思考のリアルタイム可視化** — スコア計算・LLM 推論・最終合成までのパイプラインが端末上でライブに見える。

保有ポートフォリオに対しては AI が売り時・買い増し時を判定し、その瞬間にリアルタイム通知を飛ばす。
UI/UX はプロ向けトレーディング端末（Bloomberg Terminal / 楽天 MARKETSPEED II / TradingView）のデファクトに寄せる。

---

## 1. スコープ

### In Scope
- 日本株（東証上場、銘柄コード 4 桁）のみ。
- 銘柄ピックアップ 2 系統: **中長期**（保有想定 数週間〜数ヶ月）/ **短期**（当日〜数営業日、デイトレ的運用）。
- 各ピックに **根拠（rationale）** と **確度（confidence ポイント, 0–100）** を必須表示。
- 継続的なモデル学習パイプラインと精度成長の可視化。
- 保有ポートフォリオの売買タイミング判定 + リアルタイム通知。
- プロ端末風の UI/UX。

### Out of Scope（明示的に作らない）
- **ブローカーへの実発注は一切行わない。** アプリは提案と通知のみ。約定は人間が別途手動で行う。
- 米国株・暗号資産・FX・先物。
- 投資助言業に該当する「個別断定的な投資勧誘」。出力は必ず「分析結果・参考情報」として提示し、免責を UI に常設。
- 資金移動・入出金・自動売買・自動ポジション管理。

---

## 2. 継続学習ループ（本プロジェクトの主目的 — 最優先で設計する）

### 2.1 予測台帳（Prediction Ledger）
すべてのピック・売買判定を、**予測時点で確定していた情報だけ**を使って以下とともに永続化する:

| 記録項目 | 内容 |
|:--|:--|
| `pick_id` / `issued_at`(JST) / `horizon_type` | 中長期 or 短期 |
| `symbol` / `direction` | bullish / bearish / neutral |
| `entry` / `stop` / `target` | 推奨買値・損切値・推奨売値（3 値必須） |
| `sub_scores` | technical / trend / fundamental / sentiment（各 0–100） |
| `composite_score` / `concordance` / `confidence` | 合成スコア・方向一致度・確度 |
| `feature_snapshot` | 予測に使った全特徴量のスナップショット（再現可能性のため） |
| `rationale` | 根拠テキスト（構造化 + 自然文） |
| `model_version` | 使用モデルのバージョン ID |
| `source_contributions` | 各情報源が最終スコアへ寄与した度合い |

### 2.2 決着記録（Outcome Resolution）
horizon 経過後（短期: 当日〜3 営業日 / 中長期: 5・20・60 営業日の複数ホライズン）にバッチで:
- 実現リターン、勝敗、`stop`/`target` 到達の有無と到達順序
- MFE / MAE（保有期間中の最大含み益・最大含み損）
- ベンチマーク（TOPIX）超過リターン
- `pick_outcomes` テーブルへ確信度バケット・方向コホート付きで格納。

### 2.3 評価指標（すべて時系列で保持し成長曲線にする）
- **確度較正**: 予測確度バケットごとの実測勝率、Calibration curve、Brier score。
- **ランキング有効性**: IC（スコアと前方リターンの順位相関）、precision@k。
- **ピックポートフォリオ成績**: エクイティカーブ、勝率、平均 R 倍数、期待値、最大 DD、Sharpe。
- **情報源別寄与**: 各ソースを除外した ablation で精度がどう変わるか（四半期ごと）。

### 2.4 再学習トリガ
- **定期**: 夜間（日次で軽量更新）+ 週次（フル再学習）。celery-beat で自動発火。
- **ドリフト検知**: 特徴量分布ドリフト（PSI）/ 直近 N 件の精度劣化を検知したら追加再学習。
- **ウォークフォワード検証必須** — 未来情報リークを構造的に排除。時系列 CV のみ。

### 2.5 モデルレジストリ & 昇格ゲート
- モデルはバージョン管理（champion / challenger 方式）。challenger は **shadow 推論**で本番と並走。
- 昇格条件（すべて満たす）: ホールドアウトで champion 超過 かつ 較正悪化なし かつ ピック成績（過去 M 営業日のペーパー成績）非劣化。
- 昇格・却下は**提案のみ**。自動昇格は既定 OFF（env でゲート可）。判定ログを残す。
- 確度は **isotonic / Platt scaling** で事後較正してから UI へ出す。

### 2.6 成長の可視化
- モデルバージョン別の評価指標時系列（AUC / IC / 較正誤差 / 勝率）を折れ線で。
- ピック累積成績のエクイティカーブ（中長期・短期・合算）。
- 「今週何を学習したか」= 直近再学習の差分サマリ（特徴量重要度の変化、追加された決着データ件数）。

---

## 3. AI 思考のリアルタイム可視化

推論を**ステージ化**し、各ステージの進行・中間結果を SSE / WebSocket でフロントへストリームする:

1. **データ収集** — 各情報源のフェッチ状況（成功 / 欠損 / フォールバック）をライブ表示。
2. **サブスコア計算** — technical / trend / fundamental / sentiment を個別に。各サブスコアは**特徴量レベルの寄与**（SHAP 相当）を bar で。
3. **定性オーバーレイ（LLM）** — LLM の推論をトークンストリーミング表示（思考過程が流れる「Thinking パネル」）。
4. **合成** — composite_score / concordance / confidence がどう導出されたかの内訳。
5. **3 値ブラケット決定** — entry / stop / target の算出根拠（ボラティリティ、支持抵抗、リスクリワード比）。
6. **サーバ側検証** — `stop < 現在値 < target` かつ `stop < entry < target` のチェック結果。

- すべてのトレースは `inference_traces` に永続化し、後からリプレイ可能にする。
- パイプラインは DAG ビューで表示（ノード = ステージ、状態 = pending / running / done / failed）。

---

## 4. データソース仕様

| ソース | 取得手段 | 注入ポリシー |
|:--|:--|:--|
| 株価・財務・指数 | **J-Quants**（継続利用）+ **yfinance**（継続利用） | そのまま特徴量化 |
| 日経VI 代替 | yfinance 制約により **CBOE VIX (`^VIX`)** を代替使用（Market Lens 踏襲） | 特徴量化 |
| 最新トレンド | Trend Tracking Agent（Market Lens から移植）: 自律キャッチアップ → 構造化 | 構造化データのみ注入 |
| 毎日のニュース / 市況 | Obsidian Vault `Daily/YYYY-MM-DD.md`（§4.1） | **frontmatter のみ**注入。本文は注入対策で除外。「外部由来・未検証」と明示 |
| 四季報情報 | **当面スタブ**（feature flag `SHIKIHO_ENABLED=false`）。データソース確定後に有効化（§11-1） | 確定後に frontmatter / 構造化フィールドのみ注入 |
| 銘柄ナレッジ | Obsidian Vault `Tickers/<code>_<name>.md`（§4.1） | **frontmatter のみ**注入（財務指標フィールド）。本文の「特色」等の生テキストは除外 |
| テーマ / 業種ナレッジ | Obsidian Vault `Strategies/*.md` `Industries/*.md` `Terms/*.md` | frontmatter のみ。本文は除外 |

### 4.1 Obsidian Vault

- **Vault ルート**: `C:\Users\charg\Documents\Personal Space\10_Stock`（パスは env `VAULT_ROOT` で上書き可）。
- **`Tickers/<code>_<name>.md`** — 銘柄ごとのナレッジノート。frontmatter に以下を持つ（AI 予測へ注入するのはこれらのみ）:
  `code / name / market / sector33 / sector17 / scale_cat / close_price / close_date / market_cap_oku / per_forecast / pbr / roe / equity_ratio / dividend_yield_forecast / bps / eps_forecast / dividend_forecast / shares_outstanding / last_earnings_date / last_earnings_type / data_as_of / tags`。
  本文（`## 📌 企業概要` の「特色」＝四季報類似テキスト等）は**注入しない**。
  → frontmatter の財務指標は四季報スタブ期間中の fundamental 特徴量の主データ源として使う。
- **`Daily/YYYY-MM-DD.md`** — 日次マーケットノート。frontmatter（`date / category / fact_checked / sources / type: daily-note`）のみ注入。本文の morning/evening/review ブロックは Market Lens 側が書き込む出力であり、**学習・予測の入力には使わない**（自己参照ループ防止）。
- **MOC ファイル（`00_*.md`）と `Daily/` の Market Lens 出力ブロックは無視**。
- 参照は**読み取り専用**。このプロジェクトから Vault へ書き込む場合は Market Lens と衝突しない専用ファイル / ブロックに限定し、事前にユーザー確認。

**プロンプトインジェクション防御を全ソースで徹底**: 外部由来テキストは構造化フィールドのみを LLM プロンプトへ注入し、自由記述本文は注入しない。継続学習の根拠には**自分の実測値のみ**を使う（外部テキストを学習根拠にしない）。

---

## 5. アーキテクチャ（Market Lens を踏襲しつつ拡張）

| レイヤー | 技術 | 備考 |
|:--|:--|:--|
| frontend | Next.js 16 + TypeScript (strict) / Vanilla CSS（Tailwind 禁止） | **ポート 3001**。デザイントークン単一情報源 |
| backend | FastAPI + Python 3.11（型必須, Pydantic v2 `frozen=True`, `Any` 禁止） | **ポート 8002**（Market Lens 8001 と非衝突） |
| DB | SQLite + Alembic（段階的に PostgreSQL へ移行可能な設計） | マイグレーションは head 追従 |
| 非同期 | Celery + Redis | 学習・決着バッチ・トレンド同期・通知監視 |
| リアルタイム | FastAPI WebSocket / SSE | 推論トレース配信・通知 push |
| ML | 既存（xgboost / RF / LSTM / Transformer 系）を移植 + モデルレジストリ層を新設 | 実験管理（MLflow 等）は計画時に要否判断 |
| 通知 | Web Push（VAPID）を第一候補 + 追加チャネルは §11 で決定 | アプリ内トースト + OS 通知 |
| チャート | `lightweight-charts`（TradingView OSS）等の軽量ライブラリ | ローソク足・出来高・複数時間軸 |

- ディレクトリ: `backend/{services,routers,models,ml}` / `frontend/src/{app,components,lib}` / `docs/` `data/` `plans/`。
- ファイルは 200–400 行目安・**800 行上限**。超過はモジュール分割。

---

## 6. スコアリング & 確度（Market Lens 準拠）

- technical / trend / fundamental / sentiment の各スコア（0–100）→ `composite_score`（合成）。
- `concordance` = 4 分析の方向一致度、`direction` = bullish / bearish / neutral。
- **確度（confidence ポイント）** は較正済み確率をベースにした 0–100。UI では確度バケット（高 / 中 / 低）と較正の裏付け（過去の同バケット実測勝率）を併記。
- **AI が売買銘柄を提案する全機能で 推奨買値 / 損切値 / 推奨売値 の 3 値を必須明記**。model・API・UI・通知すべてに載せる。
  サーバ側で `損切 < 現在値 < 売値` かつ `損切 < 買値 < 売値` を検証。不整合は却下 or 目安ブラケットで補完。
- ハード除外ルール（Market Lens の E1〜E3 相当を移植・調整）: SELL 判定候補をロングに混ぜない、実測勝率ゲート等。

---

## 7. ポートフォリオ売買タイミング判定 & 通知

- ユーザーが保有銘柄（数量・取得単価・取得日）を登録。
- 監視は **平日 9:00–15:30 JST**（祝日非対応、東証大引け 15:00）。
- AI が保有ごとに「継続保有 / 一部利確 / 損切 / 買い増し」を判定し、判定根拠と 3 値（更新後の stop / target、買い増し時は entry）を提示。
- トリガ発生時に**リアルタイム通知**（Web Push + アプリ内）。通知には銘柄・アクション・根拠サマリ・3 値・確度を含む。
- **承認制**: AI は提案チケットを出すだけ。約定・記録は人間が行い、結果を API で戻す（Market Lens の Human-in-the-Loop を踏襲）。
- 大引け後に EOD レビュー（当日の判定と結果の振り返り → 翌日プロンプトへ learned heuristics 注入）。

---

## 8. UI/UX 要件（プロ端末デファクト）

**参照**: Bloomberg Terminal / Refinitiv Eikon / 楽天証券 MARKETSPEED II / TradingView / SBI 証券 / 松井証券 ネットストック・ハイスピード。

- near-black ベース（`#0a0e14` 系）+ プライマリブルー（`#1f6feb` 系）。高密度・低余白。
- **騰落色は国内証券標準**: 上昇・利益 = **赤**、下落・損失 = **緑**。必ず `--color-gain`（赤）/ `--color-loss`（緑）/ `--color-flat` トークン経由。**海外仕様（緑 = 上げ）で書かない。**
- 数値は等幅相当（`font-variant-numeric: tabular-nums`）・右寄せ・カンマ区切り・小数点位置統一。
- マルチペイン / ドッキング可能なレイアウト。ウォッチリスト、板風の情報密度、複数時間軸チャート、ホットキー。
- テーブルは共通 `DataTable` コンポーネント経由（**生 `<table>` 禁止**、`caption` 必須）。
- `@media` の max-width は **640 / 768 / 900 / 1280 のみ**。
- アニメは `transform` / `opacity` のみ。
- 主要画面:
  1. **ダッシュボード** — 本日のピック（中長期 / 短期タブ）、確度順、3 値、根拠プレビュー。
  2. **銘柄詳細** — チャート + 4 分析内訳 + AI 思考トレース（ライブ / リプレイ）。
  3. **ポートフォリオ** — 保有一覧、AI 判定、通知履歴、承認キュー。
  4. **モデルラボ** — 成長曲線、較正、モデルバージョン比較、再学習ログ。
  5. **通知センター**。
- 免責表示を全画面フッタに常設。

---

## 9. 日本株ドメインルール（厳守）

- 時刻はすべて **JST**。東証大引け **15:00**、シグナル監視 9:00–15:30 平日（祝日非対応）。
- 銘柄コードは **4 桁**（例 `7203`）。`.T` サフィックスを付けない。
- 騰落色 赤 = 上げ / 緑 = 下げ（§8）。
- 日経VI は `^VIX` 代替（§4）。

---

## 10. 非機能要件

- **言語・スタイル**: コミットメッセージ・docs・コメントは日本語。コメントは What でなく **Why**。Python 全シグネチャ型付き・`async/await`。TS `any` 禁止・絶対 import（`@/...`）・関数コンポーネント + Hooks のみ。
- **テスト**: 最低カバレッジ 80%。unit / integration / E2E。TDD（RED → GREEN → REFACTOR）。DB 書き込みテストは per-file の隔離 fixture。
- **セキュリティ**: シークレットは env / secret manager。入力検証は境界で。CSP（nonce ベース）・セキュリティヘッダ。LLM プロンプトへの外部テキスト注入禁止（§4）。
- **可観測性**: 構造化ログ、推論トレース永続化、API コスト計測（LLM 呼び出しのトークン・費用を台帳化）、ヘルスチェック。
- **予算上限は安全装置であって目標ではない。自己保存を動機にしない。config・コードの自動適用はしない。**
- **性能**: ダッシュボード初期表示の主要指標は 2.5s 以内目安。リアルタイム更新はイベント駆動（ポーリング濫用しない）。
- **検証環境**: Windows / PowerShell（コマンド連結は `;`、`&&` 不可）。
- **CI**（push 前にローカル全通し）:
  - backend: `ruff check` / `black --check` / `mypy` / `pytest -q` + OpenAPI エクスポート
  - frontend: `tsc --noEmit` / `TZ=UTC jest` / `eslint .` / `next build`
- Git: `git add -A` 禁止（対象ファイルを明示 add）。フェーズ完了ごとにコミット & プッシュ（溜めない）。

---

## 11. 決定事項と残る未決事項

### 確定済み（このプロンプトに反映済み）
1. **四季報** — 当面スタブ（`SHIKIHO_ENABLED=false`）。fundamental は当面 Vault `Tickers/*.md` の frontmatter 財務指標 + J-Quants で代替。データソース確定後に有効化。
2. **通知チャネル** — **Web Push（VAPID）を既定**。追加チャネルは後日拡張（アダプタ差し替え可能な設計にする）。
3. **リポジトリ形態** — Market Lens とは**完全別リポジトリ**で新規作成。
4. **Obsidian Vault ルート** — `C:\Users\charg\Documents\Personal Space\10_Stock`（env `VAULT_ROOT`）。銘柄 = `Tickers/`、日次 = `Daily/`（§4.1）。
5. **バックエンドポート** — `8002`（Market Lens の 8001 と非衝突）。frontend は `3001`。
6. **ホライズン定義** — 短期 = 当日〜3 営業日、中長期 = 20 / 60 営業日（複数ホライズンで決着記録）。

### 残る未決事項（P0 の計画時にユーザーへ確認）
1. **ペーパートレード期間** — champion 昇格ゲートに必要な最小ペーパー成績日数（暫定 20 営業日で提案）。
2. **移植 vs 新規** — Market Lens のどのモジュールを移植し、どこから新規設計とするか。P0 の棚卸しで提案する。
3. **Web Push の対象ブラウザ / PWA 化の要否** — デスクトップ常駐で受けるか、モバイルでも受けるか。
4. **LLM プロバイダ / モデル** — Market Lens と同一構成を踏襲して良いか、コスト上限（env）をいくつにするか。

---

## 12. 運用フェーズで得た発展的機能拡張プロンプト（P9 以降の知見）

P0〜P8（本ファイル §1〜§10）の完成後、実際の運用・改善を通じて得られた発展的機能の再利用可能なプロンプト集です。同種のプロジェクトを立ち上げる際、必要に応じて追加投入してください（🔧2026-09-22、`PROJECT_CREATION_PROMPT.md` から統合・重複解消）。

### 12.1 マルチ LLM プロバイダ動的選択 & シャドウ並走
```markdown
【追加指示: マルチ LLM プロバイダ動的解決】
LLM 呼び出し層（`services/llm/registry.py`）を新設し、機能別（`stock_pick`, `portfolio_signal`, `eod_review`, `trend_analyzer`, `note_publish`）に
公式プロバイダ（Anthropic / OpenAI / Gemini）とシャドウプロバイダを環境変数および設定画面から柔軟に切り替えられるようにしてください。
シャドウプロバイダの推論結果は DB に併記保存し、UI 上で公式判定とシャドウ判定（各プロバイダ名バッジ付き）を横並びで比較表示できるようにしてください。
```

### 12.2 Point-in-Time (PIT) ファンダメンタル & センチメントスナップショット
```markdown
【追加指示: PIT スナップショットと未来リーク防止】
過去時点のファンダメンタル指標およびニュースセンチメントを再現可能にするため、日次のスナップショット台帳（`pit_fundamental_snapshots`, `pit_sentiment_snapshots`）を構築してください。
機械学習特徴量パネルを構築する際は、必ず `pandas.merge_asof(direction="backward")` を使用し、
予測日より未来のスナップショットが混入する Lookahead Bias（未来リーク）を構造的に排除してください。
また、十分な日数が蓄積されるまで学習投入をブロックする被覆率ゲート（120 営業日 / 70%）を設けてください。
```

### 12.3 note 下書き自動生成 & プレーンテキスト執筆フロー
```markdown
【追加指示: note 記事執筆ワークフロー】
当日の AI ピックと市場分析を note.com 向けに発信するパイプラインを実装してください。
- 投資助言業抵触を回避するため、note 記事向けプロンプトには売買 3 値（entry/stop/target）を渡さないこと。
- note.com エディタの自動リスト化バグやマークダウン非対応を回避するため、本文はマークダウンではなく【プレーンテキスト（記号や絵文字で見出し表現）】で出力すること。
- 表やレーダー比較は matplotlib で画像化（`docs/note/YYYY-MM-DD/images/`）すること。
- note.com への反映は下書き保存までとし、【公開ボタンは絶対に押さない】こと。
```

### 12.4 日次パイプラインログ自動集約
```markdown
【追加指示: 日次パイプラインログ集約】
毎営業日 17:00 に、候補プール全銘柄のスコア、LLM 見解、3 値、検証ゲート結果、決着、再学習結果を 1 本の Markdown に集約し、
Obsidian Vault 内（`Daily/AlphaForge/<日付>/pipeline_log.md`）へ自動保存する Celery タスクを実装してください。
```

### 12.5 並列学習アーキテクチャ（ProcessPoolExecutor）
```markdown
【追加指示: Celery Solo 下での並列機械学習】
Windows 環境における Celery の安定稼働のため Worker は `--pool=solo` を維持したまま、
複数銘柄のモデル学習や特徴量生成タスク内部で `concurrent.futures.ProcessPoolExecutor` を明示的に起動して並列化してください。
テスト実行時は子プロセスの環境引き継ぎ問題を回避するため、スレッドプールに差し替え可能な間接層（`_create_worker_pool`）を設けてください。
```

### 12.6 celery-beat の自動起動タイミング（🔧2026-09-22 修正）
上記 §2 マスター構築プロンプトの「Celery Beat は過去タスク一斉多重発火防止のため常時自動起動せず、手動制御とする」という記述は初期方針であり、そのままでは恒久的な制約ではありません。実際には、`run_picks_task` 等の定期タスク側に「休場日（土日祝、祝日は `jpholiday` 等のライブラリで判定）はスキップ」「当日分が既に生成済みならスキップ」というガードを実装すれば、celery-beat は通常のシステム起動フローに含めて安全に自動起動できます（Alpha Forge では 2026-09-20 にこの対応を行い、以降は自動起動対象に含めています）。新規プロジェクトでも、この冪等ガードの実装を P4 または P7 のタスクに含めることを推奨します。

---

## 13. 開発フェーズ計画（Claude Code はこの順で進め、各フェーズ完了時にコミット & CI 全通し）

| Phase | 内容 | 完了条件 |
|:--|:--|:--|
| **P0 調査 & 計画** | Market Lens の再利用可能モジュール棚卸し。`plans/` に PRD / アーキ / システム設計 / タスクリスト生成。§11 の未決事項をユーザーへ確認。 | 計画ドキュメント承認 |
| **P1 基盤** | リポジトリ雛形、frontend/backend/DB/Celery/Redis 起動、CI、デザイントークン、`DataTable`、免責フッタ。 | `next build` / `pytest` 通過、ローカル起動確認 |
| **P2 データ層** | J-Quants / yfinance 取得層移植、トレンド Agent 移植、ニュース digest（frontmatter のみ）、Obsidian 銘柄ノート注入、四季報スタブ。 | 各ソースの取得・欠損フォールバックの統合テスト |
| **P3 スコアリング & ピック** | 4 分析サブスコア、composite / concordance / confidence、3 値ブラケット算出 + サーバ側検証、中長期 / 短期の 2 系統ピック API。 | ピック生成 E2E、3 値整合性テスト |
| **P4 予測台帳 & 決着 & 評価** | Prediction Ledger、Outcome Resolution バッチ、評価指標（較正 / IC / 成績）、celery-beat スケジュール。 | 台帳 → 決着 → 指標算出の一連テスト |
| **P5 継続学習ループ** | モデルレジストリ、champion/challenger、shadow 推論、昇格ゲート（提案のみ）、isotonic 較正、ドリフト検知、再学習トリガ。 | ウォークフォワード検証、昇格判定ログ |
| **P6 AI 思考の可視化** | 推論ステージ化、SSE/WebSocket ストリーム、Thinking パネル、DAG ビュー、トレース永続化 & リプレイ。 | ライブトレース表示 E2E |
| **P7 ポートフォリオ & 通知** | 保有登録、AI 売買タイミング判定、承認キュー、Web Push、EOD レビュー、learned heuristics 注入。 | 通知配信 E2E、承認フロー E2E |
| **P8 モデルラボ UI & 仕上げ** | 成長曲線・較正・バージョン比較 UI、レスポンシブ確認、a11y、性能、ドキュメント（日本語）。 | 全 CI 通過、受け入れ基準充足 |

---

## 14. 受け入れ基準（Definition of Done）

- [ ] 中長期・短期それぞれのピックが、根拠 + 確度 + 推奨買値 / 損切値 / 推奨売値 付きで生成される。
- [ ] `損切 < 現在値 < 売値` かつ `損切 < 買値 < 売値` がサーバ側で強制されている。
- [ ] すべての予測が台帳化され、決着後に評価指標が自動更新される。
- [ ] 再学習が定期 + ドリフトで発火し、challenger が shadow で並走、昇格は提案のみ。
- [ ] 確度が事後較正され、UI で「同確度バケットの過去実測勝率」が確認できる。
- [ ] モデルラボで精度の成長曲線とピック累積成績が見える。
- [ ] 銘柄詳細で AI の思考パイプラインがライブに流れ、後からリプレイできる。
- [ ] 保有銘柄の売買タイミングでリアルタイム通知が届き、承認制で約定結果を戻せる。
- [ ] アプリはブローカー発注を一切行わない。
- [ ] 騰落色 赤 = 上げ / 緑 = 下げ。時刻は JST。銘柄コード 4 桁。
- [ ] 外部由来テキストは frontmatter のみ注入。学習根拠は実測値のみ。
- [ ] backend / frontend の CI がローカルで全通し。テストカバレッジ 80% 以上。
- [ ] docs / コメント / コミットメッセージが日本語。

---

## 15. Claude Code への作業指示

1. まず **P0** を実行し、`plans/` に計画ドキュメントを生成、§11 の未決事項をまとめてユーザーに確認する。**未決事項が残る項目は feature flag OFF のスタブで進める**。
2. 各フェーズは **調査 → 計画 → TDD 実装 → コードレビュー（code-reviewer / security-reviewer）→ 検証 → コミット & プッシュ** の順。溜めない。
3. 新規実装の前に必ず **Market Lens の該当モジュールを確認し、移植で足りるか判断**。ゼロから書かない。
4. 複数ファイルに及ぶ変更は計画を先に提示。可逆なリポジトリ内作業（コード編集・テスト・lint・ローカル検証・調査）は確認なしで自走。
5. コミット / push / PR / 外部送信 / 不可逆操作 / 認証情報入力 / 設定の恒常変更は従来どおり確認または明示指示を必要とする。
6. セキュリティ上の懸念（プロンプトインジェクション経路、シークレット混入、投資助言業への抵触）を見つけたら即停止して報告。
