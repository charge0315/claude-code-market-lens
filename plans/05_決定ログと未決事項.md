# 決定ログと未決事項 — Alpha Forge

> 出典: `新規プロジェクト構築プロンプト.md` §11

---

## 1. 確定事項（構築プロンプトに反映済み）

| # | 事項 | 決定 |
|:--|:--|:--|
| 1 | 四季報 | 当面スタブ（`SHIKIHO_ENABLED=false`）。fundamental は Vault `Tickers/*.md` frontmatter + J-Quants で代替。データソース確定後に有効化 |
| 2 | 通知チャネル | **Web Push（VAPID）を既定**。追加チャネルはアダプタ差し替えで後日拡張 |
| 3 | リポジトリ形態 | Market Lens とは**完全別リポジトリ**（`claude-code-market-lens`）|
| 4 | Obsidian Vault ルート | `C:\Users\charg\Documents\Personal Space\10_Stock`（env `VAULT_ROOT`）。銘柄 = `Tickers/`、日次 = `Daily/` |
| 5 | ポート | backend `8002` / frontend `3001` |
| 6 | ホライズン定義 | 短期 = 当日〜3 営業日、中長期 = 20 / 60 営業日（複数ホライズンで決着記録。中長期は 5 も追加取得） |

## 2. P0 でユーザー確認済み（2026-09-10）

| # | 事項 | 回答 |
|:--|:--|:--|
| 1 | champion 昇格ゲートの最小ペーパー成績日数 | **20 営業日**（暫定案どおり）。env `PAPER_MIN_DAYS=20` |
| 2 | Web Push 対象 / PWA 化 | **デスクトップ常駐のみ・PWA 不要**。Service Worker + VAPID のみ実装、manifest / インストール導線は作らない |
| 3 | LLM プロバイダ / モデル構成・コスト上限 | **Market Lens と同一構成を踏襲**（`ANTHROPIC_MODEL=claude-sonnet-5`、コスト上限は Market Lens 相当の安全装置値を移植）。`AI_PORTFOLIO_MODEL` は `ai_portfolio_*` を移植しない決定（P7a）に伴い未使用のため P8 で config から削除 |
| 4 | 移植 vs 新規 | [`00_調査サマリ`](00_調査サマリ_MarketLens移植棚卸し.md) で棚卸し。新規は 6 領域（N1 モデルレジストリ / N2 統合 Ledger / N3 事後較正 / N4 思考可視化 / N5 ドリフト検知 / N6 Web Push）に限定。他は移植を第一選択 |

## 2b. P9 以降でユーザー確認済み

| # | 事項 | 回答 |
|:--|:--|:--|
| 5 | UI 配色テーマ | 当初 CLAUDE.md 記載の near-black+ブルー（Bloomberg Terminal 調）から、ユーザー提示の参考デザインに合わせ暖色ダークブラウン×オレンジ（P18）→ 明るいクリーム色 "Organic" デザインシステム（P21）へ2回全面切替。**最終確定は明るいクリーム色**（bg `#f5ead8`/accent `#c67139`）。上げ赤/下げ緑のドメイン規約はテーマ変更と独立で変更なし。2026-09-20（P32）: ユーザーが Claude Design で作成したモックアップ（フォント・トーナルカラー・カード形式コンポーネント込みの "Organic" 完全版）を全画面へ適用。モックアップ原本はローソク足・AIピックの上昇/下降色にブランドのオレンジ/オリーブ（テラコッタ/セージ）を使っていたが、**上げ赤/下げ緑のドメイン規約を優先しそこだけ据え置いた**（色調自体は変更可の前提のため矛盾ではない） |
| 6 | 保有銘柄の売却時の平均取得単価 | 売却価格はユーザー入力・平均取得単価はロットの `avg_cost` をそのまま使用（別値を売却時に指定させない）。実現損益 = `(sell_price - avg_cost) * quantity`。全量売却でロット削除・一部売却で残数へ更新（P26） |

## 2c. マルチLLMプロバイダ選択（2026-09-16）でユーザー確認済み

| # | 事項 | 回答 |
|:--|:--|:--|
| 7 | 選択対象の範囲 | 公式パイプライン（実際の売買判定を左右する側）も差し替え可能にする。従来「公式=Anthropic固定・シャドウ=Gemini固定」だった構成を、`services/llm/registry.py`（`resolve_feature_provider`/`resolve_shadow_providers`）で機能ごとに動的解決する形へ一般化。Anthropic/OpenAI/Geminiの3プロバイダに対応（OpenAIを新規追加、`services/openai_client.py`） |
| 8 | 選択粒度 | 機能ごとに個別選択（`stock_pick`/`portfolio_signal`/`eod_review`/`trend_analyzer`）。env は `LLM_PROVIDER_<FEATURE>`（公式・単一）/ `LLM_SHADOW_PROVIDERS_<FEATURE>`（シャドウ・カンマ区切り複数可）。設定画面にも機能別セクションを追加（`frontend/src/components/settings/LLMProviderPanel.tsx`） |
| 9 | 反映方式 | 既存の外部APIキー設定と同じ `.env` 永続化 + 再起動反映方式を踏襲（ホットリロードは実装しない） |

既定値は導入前の実動作（公式=Anthropic固定、シャドウ=stock_pick/portfolio_signalのみGemini、eod_review/trend_analyzerはシャドウなし）と完全一致させ、未設定ユーザーへの挙動変更はゼロ。`portfolio_signal_shadows` テーブルは従来 `signal_id` 単独UNIQUEで shadow 1件までだった制約を `(signal_id, challenger_version)` 複合UNIQUEへ変更（migration `0008`）し、複数シャドウプロバイダの併用記録に対応。

**フォローアップ対応（2026-09-16 同日）**: stock_pick のシャドウ比較表示UIも汎用化した。`GeminiPicksBoard.tsx`→`ShadowPicksBoard.tsx`、`GET /api/picks/gemini`→`GET /api/picks/shadow`、`GeminiPickSummary`→`ShadowPickSummary`、`services/picks/gemini_picks.py`→`shadow_picks.py`。行ごとに`challenger_version`から動的にプロバイダ名バッジを表示するため複数プロバイダ併用時も正しく区別表示される（`frontend/src/lib/llmProviderLabels.ts`に表示ラベル変換を一本化）。あわせて `PicksBoard.tsx`/`PickDetailPanel.tsx` 等の「Claude」ハードコード表記も「公式」へ一般化し、`PickDetailPanel`フッタの実際には無関係な「生成AI Claude」表記（`model_version`はML推奨エンジンのバージョンでLLMプロバイダとは無関係）を削除した。

## 2d. note下書き自動生成・投稿運用（2026-09-17）でユーザー確認済み

| # | 事項 | 回答 |
|:--|:--|:--|
| 10 | 売買価格の非掲載 | note記事は有料の情報コンテンツであり投資助言業に抵触しないよう、下書き生成には entry/stop/target を一切渡さない（`NOTE_SCHEMA`のプロンプト制約 + 生成後の軽量チェックで警告表示、ブロックはしない）。CLAUDE.mdの「3値必須」ルールはAIが売買銘柄を提案する機能が対象であり、noteは根拠共有のみのためテーマ株ピックと同様に対象外 |
| 11 | 投稿までの自動化範囲 | 生成・レビュー・承認/却下・Obsidian/SingleHTML/WordPress WXRのファイル出力までは自動化する（celery-beatでJST 08:05に自動生成）が、**note.comへの実際の投稿（公開）は自動化しない**。公式投稿APIが存在しないためブラウザ操作で下書き画面へ貼り付け、下書き保存までで止める。一括自動公開を2026-09-17に一度試したが、この運用（下書き保存で止める）に戻すと決定 |
| 12 | 再学習との関係 | note記事の文章・読者反応は継続学習ループの教師信号に使わない（CLAUDE.mdの「教師信号は自分の実測値のみ」の原則を維持）。あくまで既存台帳データ（`prediction_ledger`）の二次利用であり、新たな判断・推論は行わない |

## 2e. PIT 特徴量（P29）運用パラメータ・将来統合方針（2026-09-18）でユーザー確認済み

| # | 事項 | 回答 |
|:--|:--|:--|
| 13 | PIT センチメント収集スコープ（旧 #I） | `keyword` センチメントも**全ユニバース**（東証全銘柄）へ拡大（既定 `PIT_SENTIMENT_SCOPE=universe`）。推奨は「候補プール + 保有 + ウォッチリスト限定」（yfinance レート制限を懸念）だったが、被覆率を優先しユーザーが全ユニバース化を選択。初回収集は yfinance のレート制限次第で完了までに時間を要しうる（24h キャッシュのため翌日以降は再取得コストが下がる）。運用開始後にレート制限で問題が出た場合はスコープを `candidates` へ戻すことを検討する |
| 14 | PIT 特徴量投入の被覆率ゲート閾値（旧 #K） | 推奨の既定（60 営業日 / 非欠損率 50%）ではなく、**より厳格な 120 営業日 / 70% で確定**（`PIT_MIN_COVERAGE_DAYS=120` / `PIT_MIN_COVERAGE_RATIO=0.7`）。安定したデータで最初の投入を行うことを優先。実運用で `source_ablations` の結果を見て必要なら緩和を再検討する |
| 15 | 四季報統合時の方針（旧 #J、四季報自体は未入手のため方針のみ確定） | 四季報データソース入手時は**別特徴量グループとして段階的に統合する**（推奨どおり）。`pit_fundamental_snapshots.source='shikiho'` の新規プロバイダを追加するだけとし、被覆率ゲートを独立に掛ける。既存 `vault_frontmatter` 由来の champion をいきなり作り直さない |
| 16 | 銘柄別モデル（LSTM/Transformer）への PIT 投入方針（旧 #L、PIT 台帳は現在 0 日のため方針のみ確定） | 推奨（変化量のみ・1 年分貯まってから）ではなく、**静的スカラー（PER/PBR/ROE 等の生値）もそのまま複製して早期・実験的に試す方針**へ変更。1 年分の蓄積を待たず、`PIT_MIN_COVERAGE_DAYS`（120 営業日）を満たした時点で `build_sequence_matrix` への組み込みを別フェーズとして着手する。パラメータ増加に対して過学習しやすい点は challenger 評価（held-out AUC/Brier）で監視する |

## 2f. 決算サプライズ・予想修正モメンタム軸（2026-09-22）でユーザー確認済み

需給軸（週末信用取引残高、#2e 実装後）に続く分析軸拡張として、決算サプライズ（本決算実績 vs
直前開示の自社通期予想）・予想修正モメンタム（四半期を追うごとの自社通期予想の上方/下方修正）
を追加した。実装前に J-Quants V2 `/fins/summary` へライブ照会し、通期予想フィールド
（`FSales`/`FOP`/`FOdP`/`FNP`/`FEPS`）の実在を確認済み。ただし同エンドポイントは四半期決算短信
のみを返し、単発の「業績予想の修正に関するお知らせ」は捕捉できないという制約も判明した
（`services/scoring/earnings_surprise_analyzer.py` の docstring に明記）。

| # | 事項 | 回答 |
|:--|:--|:--|
| 17 | composite_score/SubScores への統合可否 | 需給軸と同じ「llm_overlay 段階の追加判断材料」として配線（プロンプト＋思考トレース＋PIT台帳のみ、composite_score／確度較正／promotion gate には統合しない）。J-Quants の構造化数値のみで自由記述本文が無いため隔離LLM呼び出しも不要 |
| 18 | 対象ホライズン | 需給軸（中長期ピック限定）とは異なり**短期・中長期の両方**を対象とする。決算サプライズは発表直後の値動きに直結するイベントドリブン材料のため、ニュースセンチメントと同じくホライズンによる gate なしで常時取得する |

## 3. 残る未決事項（実装フェーズで都度確認、当面は feature flag OFF / スタブで進行）

**2026-09-18 整理**: B（EVO移植範囲）/ C（chat_service等除外）/ D（MLflow不採用）/ F（Redis DB分離）/
G（Vault書き込みマーカー）は as-built で既に決着済みであることをコード確認（`weekly_learning_service.py`
`config.py` の `redis://.../4,5` `vault_writer.py` 等）。実質「未決」ではなく整理漏れだったため
本表から除去した。旧 #I・#K（PIT運用パラメータ）は §2e で確定済みのため同様に除去した。
旧 #J・#L（四季報統合方針・銘柄別モデルへのPIT投入方針）は「発生時にどう動くか」の**方針自体は
§2e #15/#16 で確定済み**だが、トリガー事象（四季報データソース入手／PIT台帳の蓄積）が未到来のため
実行は保留のまま本表に残す。

| # | 事項 | 暫定方針 | 確認タイミング |
|:--|:--|:--|:--|
| A | 四季報データソース | `SHIKIHO_ENABLED=false` のまま**調達を保留**（2026-09-18、ユーザー確認）。`https://shikiho.toyokeizai.net/screening` からのヘッドライン（業績欄見出し【絶好調】【独自増額】等）自動取得を検討したが、同サイトの有料会員利用規約 [第18条1項9号](https://shikiho.toyokeizai.net/terms) が「コンピューター等を利用して自動取得する行為」を明示的に禁止行為として列挙しており、第17条3項には複製・公衆送信違反時の違約金条項（最低360万円）がある。robots.txt は全許可だが会員規約の方が優先されるため、**このサイトを対象にした自動収集は実装しない**（今後同じ提案が出ても再度スクレイピングを試みないこと）。データソース入手時は §2e #15 の方針（別特徴量グループとして段階的統合）で `ShikihoProvider` を追加する方針自体は維持。代替候補: 東洋経済の法人向けデータサービス（`https://biz.toyokeizai.net/data/`、正規ライセンス契約）、またはユーザーが手動で Vault frontmatter へ転記する運用（自動収集タスクには乗せない） | データソース入手時（法人データサービス契約 or 代替ソース確定時）|
| E | PostgreSQL 移行 | 当面 SQLite。スキーマは移行可能な設計を維持 | 運用データ量増加時 |
| H | 認証（Market Lens の `auth.py` 単一ユーザー方式）踏襲可否 | `config.py` の `ML_USERNAME`/`ML_PASSWORD_HASH`/`ML_SECRET_KEY`（fail-fast 検証スロット）のみ踏襲し、実際のログイン enforcement（`auth.py` router・`Depends(get_current_user)`）は実装しなかった。CLAUDE.md の「デスクトップ常駐・単一ユーザー前提」により、全 API が未認証のまま P8 まで完成し支障が無かったため。P20 の設定画面（外部APIキー編集）も同じ無認証の前提を引き継ぐ（同一 LAN 上の他端末からも `.env` を書き換えられる状態のため、外部公開時は要再検討） | P1（決定） / P8（as-built で確定と確認） / P20（設定画面追加時も踏襲を再確認） |
| J | **四季報有効化（#A）と PIT 台帳の関係**（P29） | 方針確定済み（§2e #15、別特徴量グループとして段階的統合）。実行は #A のトリガー待ち | 四季報データソース入手時（#A と同時）|
| L | **銘柄別モデル（LSTM/Transformer）への PIT 投入**（P29） | 方針確定済み（§2e #16、静的スカラーも含め早期・実験的に試す）。実行は `PIT_MIN_COVERAGE_DAYS`（120営業日）達成待ち | PIT 台帳が 120 営業日分貯まった時点 |

## 4. 変更履歴

| 日付 | 内容 |
|:--|:--|
| 2026-09-10 | P0 計画一式を作成。§11 未決 3 件をユーザー確認、`git init` 実施 |
| 2026-09-12 | P0〜P8 全フェーズ完了。§2-3・#3/#H を as-built に合わせて更新（`AI_PORTFOLIO_MODEL` 未使用のため config から削除、認証 enforcement は結局未実装のまま完成）|
| 2026-09-13 | P9〜P26 完了（銘柄別モデル自動学習・ナレッジベース統合・UI改善・マルチLLM判定・ダッシュボード刷新・配色テーマ2回全面切替・設定画面・Gemini/Claudeピック分離・ポートフォリオCRUD拡張）。§2b を新設し配色テーマ最終確定（明るいクリーム色）・売却時の実現損益計算方式を追記、#H に P20 設定画面の無認証前提の踏襲を追記 |
| 2026-09-16 | LLMプロバイダの機能別選択・併用機能を追加（`services/llm/` 共通層新設、OpenAI対応、設定画面拡張）。§2c を新設し決定事項を記録 |
| 2026-09-17 | note下書き自動生成・エクスポート機能（Obsidian/SingleHTML/WordPress WXR）とVaultアーカイブレポート機能を追加（`services/notes/` `services/vault_report/` 新設、LLMプロバイダ選択に5番目の機能キー `note_publish` を追加）。celery-beatにJST 08:05/08:15の自動生成タスクを追加し、寄り付き前ピック生成の開始時刻を07:40→07:30へ前倒し。§2d を新設し投稿運用（下書き保存までで止める）の決定を記録（`plans/04_タスクリスト.md` P27/P28 参照） |
| 2026-09-18 | Vault 由来ファンダメンタル・ニュースセンチメントの ML 学習統合（P29）を設計。ギャップ分析で「`feature_engineering.py` / `panel_feature_service.py` の特徴量はテクニカル指標のみで、ML ファクターが fundamental/sentiment を一切見ていない」「`orchestrator._feature_snapshot()` に生特徴量が残っていない」「Vault `Tickers/*.md` が上書き更新のため過去の時点情報を復元できない（lookahead bias）」の 3 点を確認。[`03_システム設計`](03_システム設計.md) に §1.8（`pit_fundamental_snapshots` / `pit_sentiment_snapshots`）・§1.9（`feature_snapshot` 粒度拡張）・§3.7（PIT 特徴量の ML 統合）を新設、§3.6 の beat 表と §6 の env 表を拡張。§3 未決 #I〜#L を追加（スコープ / 四季報連携 / 投入タイミング / 銘柄別モデル）|
| 2026-09-18 | P29a〜f を実装（`plans/04_タスクリスト.md` P29 参照）。DB マイグレーション `0010_pit_snapshots`、`pit_provider.py`/`pit_snapshot_service.py`/`pit_feature_service.py`（as-of backward join、`pandas.merge_asof(direction="backward")` で未来スナップショット混入を構造的に防止）、`panel_feature_service.build_panel` へのブートストラップ被覆率ゲート統合、`source_ablations` 初実装（`ablation_service.py`）、モデルラボ UI（`PitCoveragePanel`/`PitCoverageSummaryLine`/`AblationPanel`、`GET /api/registry/pit-coverage`・`/ablations`）。backend pytest 858件・frontend jest 338件 green、ruff/black/mypy/bandit/tsc/eslint/next build 全通過。§3 #I〜#L の未決事項は運用開始後（PIT 台帳が `PIT_MIN_COVERAGE_DAYS` 分貯まった時点）に再確認する |
| 2026-09-18 | §3 の残る未決事項をユーザーと棚卸し。B/C/D/F/G は as-built で既に決着済みと確認し表から除去（EVO移植なし・chat_service等除外・MLflow不採用・Redis DB分離・Vault専用マーカー書き込み、いずれもコード確認済み）。旧 #I・#K（PIT 運用パラメータ）はユーザー確認のうえ確定し §2e へ移動: センチメント収集スコープを推奨（候補プール+保有+ウォッチ）ではなく**全ユニバース**へ拡大（`PIT_SENTIMENT_SCOPE=universe`）、被覆率ゲートを推奨（60営業日/50%）より**厳格化**（`PIT_MIN_COVERAGE_DAYS=120`/`PIT_MIN_COVERAGE_RATIO=0.7`、`config.py`/`.env.example` 反映済み）。旧 #J・#L は「発生時の方針」を §2e #15/#16 で確定（#J は推奨どおり別特徴量グループ段階統合、#L は推奨〔変化量のみ〕ではなく静的スカラーも早期に試す方針へ変更）、トリガー未到来のため実行は §3 に残置 |
| 2026-09-18 | #A（四季報データソース）調達を検討: ユーザーから `https://shikiho.toyokeizai.net/screening` の見出し（業績欄見出し【絶好調】【独自増額】等）自動取得の依頼があったが、同サイトの有料会員利用規約 第18条1項9号が「コンピューター等を利用して自動取得する行為」を禁止行為として明記し、第17条3項に複製・公衆送信違反の違約金条項（最低360万円）があることを確認したため実装を辞退。ユーザーの判断で**調達は保留のまま継続**（`SHIKIHO_ENABLED=false` 据え置き）。同サイトを対象にした自動収集は今後も提案しない方針を #A 行に明記 |
| 2026-09-18 | ユーザー依頼で「外部由来テキストをプロンプトへ注入しない」境界（CLAUDE.md）を全ソース棚卸し。`services/vault/`・`services/picks/prompt.py`・`llm_news_sentiment_service.py`・`knowledge_search_client.py` は意図どおりだったが、`services/data/trend/analyzer.py` 経路に抜けを発見: yfinance の生ニュース見出し（外部由来・未検証）を読んだ LLM が生成する自由記述（`theme_name`/`summary`/`RelatedTicker.correlation_rationale`）が、`services/data/trend/context.py` を経由してそのまま `propose_stock_pick` 本体プロンプトへ転送されていた。`llm_news_sentiment_service.py` が `reasoning`（自由記述）を明示的に転送除外しているのと同水準の防御が欠けており、しかも波及先が実売買提案という点でより影響が大きい修正対象だった。`context.py` の `render_trends`/`_line` を数値・enum・検証済み銘柄コードのみ（`trend_id`/`lifecycle_stage`/`impact_horizon`/`momentum_score`/`sentiment_score`/`related_tickers.ticker`）に絞る形へ修正し、`test_trend_agent.py` に自由記述が転送されないことを固定するテストを追加（`theme_name`/`summary` 自体は `/api/trend`（UI表示専用）にはそのまま残す）。CLAUDE.md・`docs/architecture.md`・`plans/02_アーキテクチャ.md` の境界記述も本経路を含む形へ更新 |
| 2026-09-18 | ユーザー依頼で日次パイプラインログ機能を追加（P30、`plans/04_タスクリスト.md` 参照）。候補プールで選出した銘柄一覧・絞り込まれた銘柄一覧・4分析+MLの結果・合成スコア・LLM深掘りログ/見解・3値ブラケット・検証ゲート結果・予測台帳・決着記録・評価指標・再学習結果・モデル入れ替えの有無を1本のMarkdownへまとめ celery-beat（JST 17:00）で `Daily/AlphaForge/<日付>/pipeline_log.md` へ自動保存する。候補プール全銘柄の4分析+MLスコアは従来永続化されていなかったため `pick_pool_snapshots`（migration `0011`）を新設（ユーザー確認: 候補プール全銘柄を保存する案を選択）。同時にポートフォリオの承認待ちチケット重複（保有銘柄の場中5分おき再評価で古い `proposed` 判定が積み上がる問題）を是正: 新規判定挿入直前に同一銘柄の未承認判定を削除する方式（ユーザー確認: 削除する案を選択）。backend pytest 872件（新規14件）green、ruff/black/mypy/bandit全通過、OpenAPI再エクスポート（P29既存エンドポイントの反映漏れも合わせて解消） |
| 2026-09-20 | ユーザー依頼で学習対象設定・並列学習・ローソク足チャートのGC/DC・MACDクロスマーカーを追加（P31、`plans/04_タスクリスト.md` 参照）。事前にユーザー確認した設計方針: (1) 並列化はCelery worker自体（`--pool=solo`）を変えず、学習バッチタスク内部で`ProcessPoolExecutor`を使う、(2) 兆候検出はSMA(5/25)のGC/DCに加えMACD/Signalクロスも含める、(3) 学習対象設定は個別銘柄モデルとプールモデルの両方に適用する、(4) 「ピックした銘柄」モードは直近N日（既定30日、コード上の定数）累積。並列化の実装過程で、`ProcessPoolExecutor`の`spawn`が子プロセスを新規インタプリタとして起動するため、既存テストの`monkeypatch`によるモデル保存先隔離（`_isolated_per_ticker_model_dir`）が子プロセスへ引き継がれない問題を発見し、`_create_worker_pool`をテストから差し替え可能な間接層にして解決（本番は実`ProcessPoolExecutor`、テストはデフォルトスレッドプール）。backend pytest 912件（新規約40件）green、frontend jest 360件green、ruff/black/mypy/bandit/tsc/eslint/next build全通過。作業中に発見した無関係な既存バグ（`test_pipeline_log_service.py`のchampion_swap_count集計）は別タスクとして切り出した |
| 2026-09-20 | ユーザーが Claude Design で作成したデザインモックアップ（`AlphaForge.dc.html`、Claude Design 連携が認証エラーのため手動エクスポートされたバンドルファイルから抽出）を全画面へ適用（P32、`plans/04_タスクリスト.md` 参照）。§2b #5 に判断根拠を追記。あわせてユーザー依頼でダッシュボードへポートフォリオ概況（評価額合計/評価損益/騰落率+保有銘柄プレビュー）を新設し、ポートフォリオ画面本体のトータル収支を大きなCaprasimo見出しで強調表示。作業の過程でP31完了条件に記載していた既知の無関係バグ（`test_pipeline_log_service.py`のchampion_swap_count、テスト作成日`_DATE`固定 vs `set_champion`の現在時刻書き込みのズレでテスト作成日以外は必ず失敗する構造的バグだったと判明）と、承認キュー（`SignalQueue.tsx`）が`shadow_signals`欠落レスポンスでクラッシュしE2Eが不安定化していたバグを解消し、CIを全ジョブ（Backend 3.11/3.13・Frontend・E2E）green に復旧させた。backend pytest 926件green・frontend jest 367件green、ruff/black/mypy/bandit/tsc/eslint/next build全通過 |
| 2026-09-22 | ユーザー依頼で「celery-beat を自動起動対象外とする」CLAUDE.md の方針（2026-09-14 インシデント起因）を再点検。当時の根本原因（`run_picks_task` に休場日判定・当日重複防止ガードが無かったこと）は `8495507`/`eb3d935`/`48354f5` で解消済みと確認（`backend/tests/test_run_picks_task_idempotency.py` で固定）。加えてユーザー自身が `scripts/register-startup-task.ps1`（`AlphaForge-Autostart` タスク、本機で登録確認済み）でログオン時に celery beat を含む4プロセス一括自動起動する運用へ既に移行しており、旧方針と矛盾していたため CLAUDE.md の「ローカル起動」節を更新: celery beat を確認・起動対象に含める（週次タスクの重複発火リスクは提案書き込みのみのため許容と明記）|
| 2026-09-22 | ユーザー依頼「ローソクチャートの粒度はもっとアゲられないか」を受け、`/chart` 画面限定で足種選択（日足/60分足/15分足）を追加（P33、`plans/04_タスクリスト.md` 参照）。銘柄詳細画面（`StockOverview`）・ダッシュボードのピック詳細（`PickDetailPanel`）はチャートが補助情報という位置づけ（CLAUDE.md）のため日足のみのまま据え置き、`PriceChart` に追加した `enableIntervalSelector` prop で `/chart` 画面（`ChartPanel`）にのみ有効化した。GC/DC・MACDクロスの兆候イベントは日付（YYYY-MM-DD）単位のキーで検出しているため、同日内に複数本並ぶ分足では検出しない設計にした（無理に対応すると日付キー衝突でマーカー突き合わせが破綻するため）。Yahoo Finance の分足取得可能期間の実測上限（60分足=730日・15分足=60日）に収まるよう期間選択肢を足種ごとに制限。backend pytest 970件green（新規4件）・frontend jest 387件green（新規7件）、ruff/black/mypy/bandit/tsc/eslint全通過、ブラウザ実機確認済み。 |
| 2026-09-22 | ユーザー依頼「MDファイルと実装の差異がないか確認して」を受けドキュメント横断監査を実施。(1) `祝日は非対応`という記述が `CLAUDE.md`・`docs/operations.md`・`plans/01_PRD.md` に残っていたが、実際は2026-09-20（`48354f5`）に `run_picks_task`/`run_portfolio_monitor_task` へ `jpholiday` 判定を導入済みだったため各ファイルを修正（この2026-09-20の修正自体、当時 `plans/04`/`plans/05` への記載が漏れていたドキュメント負債だったと判明）。(2) `NOTE.md`・`CLAUDE_HARNESS.md` に残っていた「celery-beatは自動起動しない/絶対禁止」という旧方針の記述を、当日先に更新した現行方針（起動対象に含める）に合わせて修正。(3) `docs/operations.md` §6「既知の制限」の「start.ps1/stop.ps1相当は未整備」という記述が2026-09-13（`baa5e62`）の追加以降ずっと古いまま残っていたため修正。(4) `plans/03_システム設計.md` §4.1 の画面一覧が「6つ」のままでP8c（🆕）で新設した `/chart` 画面が一度も追記されていなかったため7画面へ修正し追加、`docs/architecture.md` のディレクトリ構成一覧にも `chart/` を追記。いずれもドキュメントのみの修正でコード変更なし。 |
| 2026-09-22 | ユーザー依頼「MDファイルに重複記述があるから分類・整理し、不要なものは削除して」を受け全MDファイル（16件）を棚卸し。`plans/`（P0成果物、フェーズ完了時点で凍結）と `docs/`（as-built、継続更新）は「計画 vs 実態」の異なる役割で共存させる設計と確認しそのまま維持。実質的な重複・陳腐化を3件発見し解消: (1) `CLAUDE_HARNESS.md`（2026-09-22追加）は `CLAUDE.md` の運用ルールをほぼ丸ごと重複させた「ハーネス仕様書」で、かつどの設定からも参照されておらず、celery-beat方針の更新（本日の先行コミット）が反映されず内部矛盾を起こしていた（同ファイル内でテーブル行と本文が食い違う状態）。ドキュメントとしての価値がある未収載情報（kb_creator未起動時のピック生成遅延の症状）だけ `docs/operations.md` §4.5 へ移してから削除。(2) `DESIGN.md`（P11で `tokens.css` から抽出したスナップショット）が、P21の「Organic」配色（クリーム`#f5ead8`+オレンジ`#c67139`）移行以降更新されておらず旧配色（near-black `#0a0e14`+ブルー`#1f6feb`）のまま放置されていた。単一情報源である `tokens.css` と乖離した陳腐なコピーのため削除。(3) `PROJECT_CREATION_PROMPT.md`（CLAUDE_HARNESS.mdと同一コミットで追加）は、CLAUDE.mdが「原本」と明記する `新規プロジェクト構築プロンプト.md` とほぼ同内容の構築プロンプト集だったが、運用フェーズ（P9以降）で得た知見の追加プロンプト（マルチLLM/PIT/note生成/パイプラインログ/並列学習/celery-beat冪等化）という独自価値のある部分だけ持っていた。その独自部分を `新規プロジェクト構築プロンプト.md` §12として統合（既存§12以降は§13〜§15へ繰り下げ）してから削除。3ファイルとも `git rm` のため削除内容は git 履歴から復元可能。 |
| 2026-09-22 | ユーザーから四季報オンライン（`https://shikiho.toyokeizai.net/market/N225`）のチャートを参考にしてほしいと依頼。機能棚卸し後にユーザー確認: 「移動平均線の実線描画＋一目均衡表/ボリンジャーバンド切替」「下段サブインジケーター（出来高/MACD/RSI/ストキャスティクス）」「マルチチャート（既存の日足/60分足/15分足の3種のみ対象、週足/月足/年足は対象外）」の3点を採用（P34、`plans/04_タスクリスト.md` 参照）。本セッションでは前2点を実装完了、マルチチャートは未着手のまま持ち越し。並行してユーザーから2件の追加要望「候補プール抽出の経緯をもっと詳しく」「LLM深堀りログを全出力（シャドウ含めて比較）」を受け調査を実施し、実装前にギャップを報告済み（未実装）: 候補プール一覧（`pick_pool_snapshots`）はDBにはあるがREST API未実装、絞り込み条件自体も「値上がり/値下がり/出来高TOP50→重複除去」という単純ランキングのみで出来高・セクター等のフィルタは元々存在しない。LLMは弊社設計上そもそも2〜4文・1024トークン上限でしか根拠を生成させておらず（extended thinking不使用）「深掘りログの全出力」に対応する長い思考過程はシステム内に存在しない。プロンプト全文も生成直後に破棄されDB保存されない。シャドウLLM比較機能自体は`PickDetailPanel`に実装済みと判明。さらに続けてユーザーから「chart画面で銘柄指定→4分析〜検証ゲートの経緯をライブログ出力したい」という要望があり、対象銘柄の範囲（今日のAIピック対象のみ vs 任意銘柄でその場から新規推論）を確認したところ「任意銘柄でその場から新規推論を実行」を選択。これは prediction_ledger を汚さない専用の「試し打ち」モード・LLM_DAILY_COST_LIMIT_USD管理下でのクリックごとの課金発生という設計上の論点を伴うため、実装前に設計案を提示する方針（未実装）。 |
