# タスクリスト — Alpha Forge

> 出典: `新規プロジェクト構築プロンプト.md` §12 / [`03_システム設計`](03_システム設計.md)
> 各フェーズ: 調査 → 計画 → TDD 実装 → コードレビュー（code-reviewer / security-reviewer）→ 検証 → コミット & プッシュ。溜めない。
> 新規実装の前に必ず Market Lens の該当モジュールを確認し移植で足りるか判断する。

進捗記号: `[ ]` 未着手 / `[~]` 進行中 / `[x]` 完了

---

## P0 — 調査 & 計画 ★現在ここ

- [x] Market Lens リポジトリ構成・主要サービスの棚卸し（[`00_調査サマリ`](00_調査サマリ_MarketLens移植棚卸し.md)）
- [x] Obsidian Vault `10_Stock` の `Tickers/` `Daily/` frontmatter 実物確認
- [x] §11 未決事項をユーザーへ確認（ペーパー 20 営業日 / デスクトップ常駐のみ PWA 不要 / LLM は Market Lens 同一構成）
- [x] `plans/` に PRD / アーキテクチャ / システム設計 / タスクリスト / 決定ログを生成
- [x] `git init`（新規リポジトリ）
- [x] **計画ドキュメントのユーザー承認**（← このタスクの完了条件）
- [x] 承認後: `plans/` を初回コミット

**完了条件**: 計画ドキュメント承認。→ 完了（2026-09-11、コミット `3e9f2d3`）

---

## P1 — 基盤 ✅ 完了（2026-09-11）

- [x] リポジトリ雛形（`backend/` `frontend/` `docs/` `data/` サブパッケージ構成、[`02_アーキテクチャ`](02_アーキテクチャ.md) §2）
- [x] Alpha Forge 専用 Python venv（3.11）、`backend/requirements.txt` / `requirements-ci.txt`（Market Lens ベース、ML ライブラリは P3/P5 で追加）
- [x] `backend/config.py`（pydantic-settings、[`03_システム設計`](03_システム設計.md) §6 の env。CSV env は `NoDecode` + validator）
- [x] FastAPI `main.py`（ポート 8002）、`/health`（DB / Redis 疎通）+ `/api/health`（liveness）
- [x] Celery + Redis（専用 DB 番号 4/5）、`celery_app.py` / `tasks.py` 雛形、beat スケジュール枠
- [x] Alembic 初期化 + 新 baseline マイグレーション（[`03`](03_システム設計.md) §1 の全 16 テーブル、upgrade/downgrade テスト付き）
- [x] frontend 雛形（Next.js 16、ポート 3001）、`tokens.css`（騰落色 赤=上げ/緑=下げ）、`DataTable`（生 table 禁止・caption 必須）、SSE/WS クライアント雛形、免責フッタ、5 画面スタブ
- [x] `.github/workflows/ci.yml`（backend: ruff/black/mypy/bandit/pip-audit/pytest+cov/openapi、frontend: eslint/tsc/jest/build、e2e: Playwright smoke）
- [x] `.gitignore` / `.env.example` / `README.md`（日本語）/ `CLAUDE.md`（Alpha Forge 版）
- [x] セキュリティヘッダ（backend）+ nonce ベース CSP（`frontend/src/proxy.ts`、Next 16 で middleware→proxy 改称）

**完了条件**: `next build` / `pytest` 通過、ローカルで backend 起動確認。
→ backend: ruff/black/mypy/bandit/pytest(14) 緑、uvicorn 起動 & `/api/health` 応答確認。
→ frontend: tsc/eslint/jest(10)/next build 緑。
→ Celery/Redis: `celery_app` import 可、Redis は Market Lens コンテナで疎通（`/health` 200）。

---

## P2 — データ層 ✅ 完了（2026-09-11、pytest 82 緑）

- [x] `services/jst_time` / `services/trading_calendar` 移植（⭐、P2a コミット `81ecbd6`）
- [x] `services/vault/brand_notes_service` 移植（⭐、`VAULT_ROOT` 起点で `Tickers/` 導出、P2a）
- [x] `services/vault/news_digest_service` 改修移植（🔧、読み取り元を `Daily/YYYY-MM-DD.md` frontmatter へ。本文の morning/evening/review は自己参照ループ防止で読まない、P2a）
- [x] `services/vault/daily_note_service`（🔧、当面 read-only。書き込みは P7 で専用マーカー付き、P2a）
- [x] `services/data/shikiho` 四季報スタブ（`SHIKIHO_ENABLED=false` の間は常に `None`、P2a）
- [x] プロンプトインジェクション境界テスト（本文の "Ignore all previous instructions" が提示ブロック・構造化 dict に載らないことを固定、P2a）
- [x] `services/circuit_breaker` 移植（⭐、P2b コミット `ce2d3bf`）
- [x] `services/data/jquants_client` + `jquants_errors` + `models/jquants_raw` 移植（⭐、P2b）
- [x] `services/data/data_fetcher`（yfinance）+ `data_fetcher_errors` + `data/cache` + `models/stocks` 移植（⭐、P2b）
- [x] `services/data/ranking_service` 移植（⭐、候補プールの母集団、P2c コミット `<次>`）
- [x] `services/data/trend_service`（業種別週次リターン集計、LLM なし）+ `models/{dashboard,trend}` 移植（⭐、P2c）
- [x] `services/data/macro_features`（`^VIX` = 日経VI 代替）移植（🔧、P2c）
- [x] 取得・欠損フォールバックの統合テスト（retry→circuit open、契約プラン範囲外 400 フォールバック、非営業日スキップ、単一銘柄失敗の許容）

**完了条件**: 各ソースの取得・欠損フォールバックの統合テストが緑。→ 達成。

### P2 から P3 へ繰り越し（LLM 依存のため）

- [ ] `services/data/trend/`（collector / analyzer / context / sync_service）— Trend Tracking Agent の
  テーマ構造化は Anthropic クライアントに依存するため、`anthropic_client` 移植（P3）と同時に行う。
  `trend_service`（定量集計）は P2 で移植済みで、trend サブスコアはこれで算出できる。

---

## P3 — スコアリング & ピック（進行中）

- [x] **P3a**（コミット `0f933bf`）: `services/scoring/technical_analysis`（+ ATR）/ `sentiment_analyzer` 移植、`fundamental_analyzer` 🔧（`get_fundamental_with_vault_fallback` で Vault frontmatter 補完）
- [x] **P3b**（コミット `<次>`）: `services/anthropic_errors` / `anthropic_client`（🔧 forced tool-use を `_forced_tool_call` に DRY 集約、`propose_stock_pick` / `propose_trends` / `converse` を移植）/ `api_cost`（🔧 raw sqlite → SQLAlchemy async の `db/api_cost_db` へ置換、`0002_api_costs` マイグレーション追加）
- [x] **P3c**（コミット `<次>`）: `services/scoring/signal_scan_scoring`（compute_composite ほか純関数）+ `subscores`（technical/fundamental サブスコアを分割）+ `recommender`（🔧 ML 予測を `MlScoreProvider` に置換 = 既定 `null_ml_score`、`source_contributions` 新規追加、閾値は固定 + パラメータ化）を移植。`ledger/labeling` / `learning/ts_validation` も移植（純関数、P4/P5 で使う）
  - **繰り越し**: `ml_predictor` / `ensemble_predictor` / `feature_engineering` / `dl/` と xgboost/scikit-learn/torch は **P5（継続学習ループ）** へ。fresh な Alpha Forge に学習済みモデルは無く、registry が埋まるのが P5 のため。`recommender` は provider 差し替えで実 ML に接続できる設計にしてある。BUY/SELL の動的閾値も `prediction_ledger` にデータが貯まる P4 以降で配線
- [x] **P3d**（コミット `<次>`）: ピック生成パイプライン。896 行の `stock_pick_service` は Market Lens 固有機構が多いため Alpha Forge の `prediction_ledger` 中心に再設計して移植（🔧）:
  - `picks/bracket`: 3 値ブラケットの ATR クランプ + **サーバ側検証**（`stop<現在値<target` かつ `stop<entry<target` かつ全て正値）+ 保有期間標準化（`_apply_atr_bracket` / `_validate_pick` を移植）
  - `ledger/prediction_ledger`: `prediction_ledger` テーブルの writer/reader（`feature_snapshot` 完全版 + `source_contributions` + confidence バケット）
  - `picks/prompt`: 深掘りプロンプト（**frontmatter / 構造化フィールドのみ注入**、目安ブラケット提示）
  - `picks/pipeline`: 候補プール(`ranking_service`)→`recommender`スコアリング→ショートリスト→LLM深掘り(`propose_stock_pick`)→bracket 検証→ハード除外(E1 recommender SELL / E2 value_trap 確度キャップ)→`prediction_ledger` 台帳化。中長期/短期を horizon パラメータで両対応
  - `routers/picks`: `GET /api/picks/mid-term` `short-term` `{pick_id}` / `POST /api/picks/run`
  - テスト: bracket 検証・クランプ、台帳 CRUD、E1/E2/矛盾/LLM エラー/should_include=false の各除外経路、API envelope・3 値必須。pytest 165 緑
  - **E3（実測勝率ゲート）は `pick_outcomes` が貯まる P4 以降で有効化**（コメントで明示）。動的閾値も同様
- [x] **P3e**（コミット `<次>`）: **Trend Tracking Agent**（P2 繰り越し分）を移植:
  - `models/trend_tracking` + `0003_trend_snapshots` マイグレーション + `db/trend_snapshot_db`（raw sqlite → SQLAlchemy async）
  - `services/data/trend/`（collector 🔧 `_INDEX_SYMBOLS` 内蔵化 / analyzer ⭐ `propose_trends` 経由 / sync_service 🔧 DB 置換・TTL 3h / context ⭐）
  - `render_trend_context()` をピックプロンプトへ配線（読み取りのみ、同期は beat 任せ）
  - `routers/trend`: `GET /api/trend` / `POST /api/trend/sync`
  - テスト: `_coerce_trends` の enum 既定・クランプ、master 検証、mock/LLM 分岐、TTL 再利用、context 整形。pytest 173 緑

### P3 繰り越し（P3e から先送り）

- [ ] `services/picks/theme_pick_service`（テーマ株ピック、discovery 用途・3 値対象外）+ `llm_tools`（converse ベースのツールループ）— web 検索 + マルチターン tool-use。ピック本流には不要なため P8 UI 仕上げ前後で判断
- [ ] `services/picks/trend_picks_service`（トレンド業種別ピック）— `signal_scan_db` 依存
- [ ] `services/picks/signal_scan_*`（全銘柄スキャン中断再開バッチ）— 候補プールを `ranking_service` から全ユニバースへ広げる拡張。現状 `ranking_service` で足りているため P4/P5 と並行で必要になった時点で

**完了条件**: ピック生成 E2E、3 値整合性テストが緑。→ P3d で達成（pipeline E2E + bracket 整合テスト）。

---

## P4 — 予測台帳 & 決着 & 評価 ✅ 完了（2026-09-11、pytest 193 緑）

- [x] `services/ledger/prediction_ledger`（P3d で先行実装。CL-1、`feature_snapshot` 完全版 + `source_contributions`）
- [x] `services/ledger/eval_metrics` + `skill_metrics` 移植（⭐、IC / precision@k / Brier / skill）+ Alpha Forge 追加（`spearman_ic` / `equity_curve` / `max_drawdown` / `sharpe_ratio`）。scikit-learn を requirements に追加
- [x] `services/ledger/outcome_resolver`（🔧 pick_outcome_service を再設計）: 後追い解決 + **複数ホライズン**（短期 1/2/3、中長期 5/20/60）+ **MFE/MAE** + **first_hit** + **TOPIX(^N225) 超過**。約定モデル（指値・トリプルバリア・損切り優先）は Market Lens 踏襲
- [x] `services/ledger/eval_service`: 較正曲線（予測確度バケット → 実測勝率、`is_calibrated=false` = 事後較正は P5）+ IC + precision@k + 勝率 + 平均 R 倍数 + 期待値 + 最大 DD + Sharpe を `eval_snapshots` / `calibration_curves` へ追記
- [x] `services/db/{pick_outcome_db, eval_db}`（SQLAlchemy async。`pick_outcomes` / `eval_snapshots` / `calibration_curves` は `0001_baseline` に既存、マイグレーション不要）
- [x] **E3 実測勝率ゲート**をピックパイプラインに配線（`cohort_winrate`。約定 n ≥ 20 かつコホート勝率 < 45% で hard 除外。薄いうちは休眠）— P3d の TODO を解消
- [x] celery-beat スケジュール（`run-picks-{mid,short}-term` 08:50/08:52 JST、`resolve-pick-outcomes` 16:38、`update-eval-metrics` 16:48、`sync-trends` 1 日 4 回）+ `tasks.py` 実装
- [x] API: `GET /api/ledger` `/api/ledger/{id}/outcomes` `POST /api/ledger/resolve` / `GET /api/eval/growth` `/calibration` `/equity-curve` `POST /api/eval/run`
- [x] テスト: 台帳 CRUD → 決着（sim/multi-horizon/損切り優先/time_exit）→ 指標算出（較正曲線・IC・エクイティ）の一連、E3 ゲート発動 / 休眠。OpenAPI 16 paths

繰り越し: `backtester`（ウォークフォワード検証の土台）と `pick_simulation_service`（執行シミュレーション）は P5 の昇格ゲート（ペーパー成績）で必要になった時点で移植。`source_ablations`（四半期）は情報源が出揃う P5 以降。

**完了条件**: 台帳 → 決着 → 指標算出の一連テストが緑。→ 達成。

---

## P5 — 継続学習ループ（進行中）

- [x] **P5a**（コミット `<次>`）: `services/registry/calibration`（🆕 N3）— `confidence_raw` → `win` から isotonic（n≥40）/ Platt（n≥10）/ identity（それ未満）を自動選択して学習し `data/calibrators/<scope>_<horizon>.joblib` へ永続化。`eval_service.compute_and_persist` が夜間バッチで学習し `calibration_curves.is_calibrated` を更新、ピックパイプラインが軽量ロードして `confidence_raw` に適用してから台帳化（E2/conflict キャップの後・E3 ゲート/確度フロアの前）
- [x] **P5b**（コミット `<次>`）: `services/registry/drift`（🆕 N5）— `prediction_ledger.feature_snapshot` の dotted path から特徴量系列を抽出し PSI を計算、`drift_snapshots` へ記録。監視対象 4 特徴量（technical/fundamental サブスコア・ATR・trend）。`GET /api/registry/drift`。celery-beat 週次（`run-drift-check`）
- [x] テスト隔離: 較正器（joblib）保存先を `conftest.py` の autouse フィクスチャで per-test tmp_path 化（実 `data/calibrators/` への書き込み漏れ・テスト間汚染を修正）
- [x] **P5c**（コミット `<次>`）: `services/registry/`（🆕 N1）モデルレジストリ運用 — `model_registry` / `model_champions` / `model_promotions`。「モデル」= recommender+LLM パイプライン構成のスナップショット（`MODEL_VERSION`、まだ実 ML が無いため）。lane は `model_type = recommender_llm_<lane>` に符号化（スキーマ変更なし）。lane 初回バージョンは無条件で champion をブートストラップ、以降は昇格ゲート必須。昇格ゲート（[`03`](03_システム設計.md) §3.3、ペーパー日数 ≥ `settings.paper_min_days`(20) かつ ホールドアウト勝率超過 かつ 較正非劣化(Brier) かつ 平均 R 倍数非劣化）→ `verdict`（propose_promote/hold/reject）を `model_promotions` へ記録（`applied=0`）。`POST /api/registry/promotions/{id}/apply` のみが champion を差し替える（人手承認専用、`MODEL_AUTO_PROMOTE=false` 固定）。`GET /api/registry/champions` `/promotions`、`POST /api/registry/promotions/evaluate`。celery-beat 週次（`run-promotion-evaluation`）で全 lane の challenger を自動評価（適用はしない）。`shadow_predictions`（shadow 推論の実行）は実 ML モデルが無いため P5d 以降へ据え置き — 現状 challenger の実績は「本番ピックとして生成され後から決着した実測値」を使う（walk-forward、リークなし）
- [x] **P5d**（コミット `<次>`）: `services/learning/`（🔧 移植 + 断面プールのみへスコープ限定）— `pool_labeling` / `pool_model` / `feature_engineering`（`build_feature_matrix` 回帰パスのみ、v1/classification/`build_sequence_matrix` は対象外） / `panel_feature_service`（`build_panel`/`build_panel_context`/`build_inference_row`、`predict_mode=True` 化で本番当日の推論行欠落バグを修正） / `pool_training_service`（`PoolClassifier` の XGBoost 分類器、日付単位 purged walk-forward、`train_pool_model`）。per-ticker ローテーション（`ml_predictor`/`ensemble_predictor`/`training_batch_service`、LSTM/Transformer 含む）は**意図的に対象外**（Alpha Forge の champion/challenger レジストリは pool 前提の設計になっており、per-ticker は互換しない別パラダイムが必要になる上、クロスセクショナルなプール学習の方がサンプル数・汎化の点で本質的に有利なため — 詳細は P5d 実施時の技術判断ログ参照）。`services/registry/model_registry.model_type_for_lane` に `lane="ml_pool"` → `model_type="xgboost_pool"` を追加（他 lane は無変更）。初回バージョンは無条件で champion ブートストラップ、以降は `services/registry/promotion.evaluate_ml_pool_promotion`（held-out AUC/Brier 直接比較、ピック実測ではなくオフライン検証のみ — プールモデルはピックの勝率と1対1に対応しないため）が新設ゲートとして判定、`apply_promotion`（既存 API）で人手承認。`services/scoring/ml_score_provider.py`（🆕）で `PanelContext` + champion `PoolClassifier` を閉じ込めた `MlScoreProvider` を構築し `pipeline.run_picks` へ配線（champion 未登録時は自動的に ML ファクターなしへフォールバック、フラグ不要）。celery-beat 月次（`run-pool-training`）で再学習、週次 `run-promotion-evaluation` が ml_pool lane も走査
- [x] `services/registry/factor_weight_service` 移植（コミット `<次>`、🔧 shadow）— Market Lens は別テーブル `signal_scan_ic_runs`（`signal_scan_ic_db`/`signal_scan_ic_service`、未移植の signal_scan 基盤依存）から factor-IC を読むが、Alpha Forge は本番ピックが実際に使う `recommender.FACTOR_WEIGHTS`（technical/ml_prediction/fundamental/sentiment、`_WEIGHTS` から public 化）を対象に、既存の `prediction_ledger.source_contributions` + `pick_outcomes.excess_return` から直接 `spearman_ic` を算出する（別バッチ・別テーブル不要）。サンプル不足ファクターは既定重みへフォールバック、算出値は `GET /api/eval/factor-weights` で参照できるのみで本番合成には未反映（shadow）。既定重みの spread（技術0.35:センチメント0.10=3.5倍）が Market Lens の想定（1.33倍）より広いため `_MAX_WEIGHT_RATIO` を 3.0→5.0 へ調整（🔧、フォールバックのみの状態で重みが歪まないため）
- [x] `improvement_*` の必要範囲のみ実装（コミット `<次>`、🆕 週次学習差分サマリ `GET /api/eval/weekly-learning`）— Market Lens の `improvement_*`（コード変更を自動提案し実装後に効果測定する EVO ループ、`git revert` は人間が実施）は CLAUDE.md の自律運用方針「設定・コードの自動適用はしない」と相容れず、Alpha Forge の継続学習ループ（モデル再学習・昇格ゲート・ドリフト検知のみ）とも目的が異なるため移植しない。代わりに既存の継続学習アーティファクト（`eval_snapshots`/`model_promotions`/`drift_snapshots`）から直近1週間の評価指標差分・昇格判定・ドリフト検知フラグをロールアップするだけの読み取り専用サマリを新規実装（`services/ledger/weekly_learning_service.py`）

**完了条件**: ウォークフォワード検証が通り、昇格判定ログが残る。自動昇格 OFF を固定するテスト。→ P5c/P5d で達成予定。

---

## P6 — AI 思考の可視化

- [ ] `services/inference/orchestrator`（🆕 N4、stage DAG: collect→subscore→llm_overlay→synthesis→bracket→verify）
- [ ] 各 stage を既存移植サービスの薄いラッパとして実装、`frozen` 中間 DTO
- [ ] `inference_traces` 永続化（逐次 INSERT、[`03`](03_システム設計.md) §1.6）
- [ ] サブスコアの SHAP 相当寄与を `ml_predictor` に追加、payload へ
- [ ] LLM トークンストリーミング（`anthropic_client` stream → stage ③ イベント）
- [ ] `routers/inference.py`: `GET /api/inference/runs` `/{run_id}` `/{run_id}/stream`(SSE) `/{run_id}/replay`
- [ ] frontend `components/pipeline/`: DAG ビュー（pending/running/done/failed）+ Thinking パネル + SHAP bar
- [ ] 銘柄詳細画面にライブ / リプレイ切替
- [ ] リプレイ（保存トレースを同じレンダラで再生）

**完了条件**: ライブトレース表示 E2E が緑（ステージ遷移とリプレイ一致）。

---

## P7 — ポートフォリオ & 通知

- [ ] `services/portfolio/portfolio_service` 移植（⭐、保有 CRUD）
- [ ] `services/portfolio/ai_portfolio_*` 移植 + 整理（🔧、HITL 承認制、「継続保有/一部利確/損切/買い増し」+ 3 値）
- [ ] `services/portfolio/risk_service` 移植（⭐）
- [ ] `portfolio_signals` テーブル + 承認キュー API（`approve` / `reject` / `report-fill`）
- [ ] `services/notify/notification_service` 移植（🔧、`channel` 追加）
- [ ] `services/notify/webpush`（🆕 N6、`pywebpush` + VAPID 鍵管理 + `push_subscriptions`）
- [ ] `POST /api/notify/subscribe` `/unsubscribe`、`GET /api/notify/vapid-key`、`WS /ws/notifications`
- [ ] frontend Service Worker（Web Push 受信、デスクトップ常駐。**PWA manifest / インストール導線は作らない**）
- [ ] 保有監視 beat（平日 9:00–15:30 JST、祝日非対応）→ トリガ時 Web Push + アプリ内
- [ ] `eod_reviews`（大引け後、learned heuristics を翌日プロンプトへ注入）

**完了条件**: 通知配信 E2E、承認フロー E2E が緑。

---

## P8 — モデルラボ UI & 仕上げ

- [ ] frontend モデルラボ: 成長曲線（AUC/IC/較正誤差/勝率）、Calibration curve、バージョン比較、再学習ログ、PSI ドリフト、週次学習差分
- [ ] frontend ダッシュボード / 銘柄詳細 / ポートフォリオ / 通知センターの仕上げ
- [ ] レスポンシブ（320/375/768/1024/1440/1920、overflow なし）、a11y（キーボード / コントラスト / reduced-motion）
- [ ] 性能（ダッシュボード初期表示 2.5s 目安、イベント駆動更新）
- [ ] 免責フッタ全画面確認、投資助言業に触れない文言レビュー
- [ ] ドキュメント（`docs/` 日本語、architecture_*.md / operations.md）
- [ ] 全 CI 通過、カバレッジ 80% 以上、受け入れ基準（[`01_PRD`](01_PRD.md) §8）チェック

**完了条件**: 全 CI 通過、受け入れ基準充足。

---

## 横断（各フェーズで随時）

- [ ] 移植モジュールの docstring に「Market Lens `services/<name>.py` から移植、変更点: …」を Why として記載
- [ ] 800 行超モジュールは移植と同時に分割
- [ ] フェーズ完了ごとにコミット & プッシュ（`git add -A` 禁止、対象ファイル明示）
- [ ] セキュリティ懸念（インジェクション経路 / シークレット混入 / 投資助言業抵触）発見時は即停止して報告
