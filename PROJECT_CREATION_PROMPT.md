# PROJECT_CREATION_PROMPT.md — Alpha Forge プロジェクト構築マスタープロンプト集

> **このドキュメントの目的**  
> 本ファイルは、日本株 AI 銘柄ピック＆継続学習トレーディング端末「**Alpha Forge**（`claude-code-market-lens`）」と同等の高信頼・自律進化型 AI アプリケーションを、新規リポジトリから Claude Code（または同様のエージェント）を用いてゼロから立ち上げるための**指示プロンプト一式を 1 ファイルにまとめたマスタープロンプト集**です。
> 
> 初期の基盤構築から、実際の運用・改善を通じて得られた発展的機能（マルチ LLM シャドウ比較、PIT タイムカプセル特徴量、note 自動生成、並列学習基盤など）までを完全網羅しています。

---

## 目次

1. [プロンプト集の使い方と立ち上げワークフロー](#1-プロンプト集の使い方と立ち上げワークフロー)
2. [マスター構築プロンプト（プロジェクト初期投入用）](#2-マスター構築プロンプトプロジェクト初期投入用)
3. [意思決定・要件ヒアリング用プロンプト（Phase 0）](#3-意思決定要件ヒアリング用プロンプトphase-0)
4. [フェーズ別詳細実装プロンプト（Phase 0 〜 Phase 8）](#4-フェーズ別詳細実装プロンプトphase-0--phase-8)
5. [発展的機能拡張プロンプト（運用フェーズの知見）](#5-発展的機能拡張プロンプト運用フェーズの知見)
6. [完成受け入れ基準（Definition of Done）](#6-完成受け入れ基準definition-of-done)

---

## 1. プロンプト集の使い方と立ち上げワークフロー

### 1.1 立ち上げの手順
1. **新規リポジトリを作成**し、ルートディレクトリでシェルを開く。
2. 本ファイル内の [「2. マスター構築プロンプト」](#2-マスター構築プロンプトプロジェクト初期投入用) をコピーし、Claude Code の初回セッションに投入する。
3. Claude Code はまず **Phase 0（調査と計画策定）** を自律実行し、`plans/` 配下に設計ドキュメント一式を作成する。
4. [「3. 意思決定・要件ヒアリング用プロンプト」](#3-意思決定要件ヒアリング用プロンプトphase-0) に記載された未決事項についてユーザーと Claude Code 間で合意を形成する。
5. 以降、[「4. フェーズ別詳細実装プロンプト」](#4-フェーズ別詳細実装プロンプトphase-0--phase-8) を順次適用しながら、各フェーズごとに TDD 実装 → CI 全通し → コミットを進める。

### 1.2 Claude Code への運用指示ルール
- **可逆作業の自走**: コード編集、テスト実行、型チェック、リンタ、調査は確認不要で自走する。
- **不可逆操作の確認**: Git コミット・プッシュ、本番外部サービス連携、ファイル削除などは必ず確認を取る。
- **環境制約の遵守**: Windows PowerShell ではコマンド連結に必ず `;` を使用する（`&&` は禁止）。

---

## 2. マスター構築プロンプト（プロジェクト初期投入用）

新規リポジトリのルートで Claude Code を起動し、以下のプロンプトをそのまま投入してください。

```markdown
あなたはプロのシニア金融エンジニア兼フルスタック AI アーキテクトです。
以下の要件定義に従い、日本株を対象とした「AI 銘柄ピック & 継続学習トレーディング支援端末」（コードネーム: Alpha Forge）を新規リポジトリ内にゼロから設計・構築してください。

最初にいきなりコードを書くのではなく、【Phase 0: 調査・計画】として `plans/` ディレクトリ配下に
PRD（01_PRD.md）、アーキテクチャ設計（02_アーキテクチャ.md）、システム詳細設計（03_システム設計.md）、
タスクリスト（04_タスクリスト.md）、決定ログ（05_決定ログと未決事項.md）を作成し、ユーザーの承認を得てください。

================================================================================
■ 0. エレベーターピッチ
複数の独立した情報源（テクニカル、トレンド、ファンダメンタル、センチメント、Obsidian ナレッジ）を統合し、
中長期銘柄と短期（デイトレ）銘柄をそれぞれ「推奨買値 / 損切ライン / 推奨売値」の 3 値付きでピックアップする
高密度プロ仕様のトレーディング支援端末。

最大のコア価値は以下の 2 点:
1. 継続学習ループ: すべての予測を台帳化し、複数営業日後の決着結果からモデルを自動再学習し成長を可視化する。
2. AI 思考のリアルタイム可視化: 収集→サブスコア→合成→LLM 推論→3値決定→検証のパイプラインがライブに流れる。

■ 1. スコープ定義
【In Scope】
- 日本株（東証上場銘柄、数字 4 桁コード）のみを対象とする。
- 銘柄ピックアップ: 中長期（数週間〜数ヶ月）および短期（当日〜3営業日）の 2 系統。
- 全提案における「推奨買値 / 損切ライン / 推奨売値」の 3 値必須化とサーバ側整合性検証。
- 事後較正（Isotonic / Platt）による確度バケットと過去実測勝率の併記。
- 予測台帳（Prediction Ledger）と決着記録（Outcome Resolution）。
- Champion / Challenger 方式による Shadow 推論と、承認制の昇格ゲート。
- 保有ポートフォリオのリアルタイム売買タイミング判定（継続/利確/損切/買い増し）と通知。
- プロ向け端末 UI（Organic テーマ: クリーム色 #f5ead8 ベース、高密度・低余白）。

【Out of Scope（絶対に実装しない）】
- ブローカーへの実発注（API 連携含む）。アプリは提案と通知のみ。約定は人間が手動で行う。
- 米国株、暗号資産、FX、先物。
- 投資助言業に該当する個別断定的な投資勧誘文言（「買い推奨」等の断定を避け、分析結果・参考情報として提示。免責常設）。
- 資金移動、自動ポジション管理。

■ 2. 厳格なドメインルール & セキュリティガードレール
1. 騰落色規約（国内証券標準）:
   上昇・利益 = 赤（--color-gain）、下落・損失 = 緑（--color-loss）。
   海外仕様（緑=上げ）の実装を厳禁とする。
2. 時刻と市場基準:
   全処理を JST 基準とし、東証の大引け 15:00、シグナル監視は平日 9:00–15:30（祝日・休場日ガード必須）。
   銘柄コードは数字 4 桁（.T は内部で除去）。
3. プロンプトインジェクション多層防御:
   Obsidian Vault ノート本文、生ニュース本文、四季報本文などの外部テキストは【絶対に LLM プロンプトへ注入しない】。
   注入するのは frontmatter や抽出・検証済みの数値・enum のみとする。
   隔離 LLM が出力した自由記述 reasoning も本体プロンプトへは転送しない（間接インジェクションのロンダリング防止）。
4. 継続学習の自己完結:
   学習の教師信号には【自システムの実測値（リターン・勝敗）のみ】を使用し、外部テキストを学習根拠にしない。
5. 3 値ブラケット整合性:
   「損切 < 現在値 < 売値」かつ「損切 < 買値 < 売値」をサーバ側で強制し、不整合は却下または補正する。

■ 3. アーキテクチャ & 技術スタック
- フロントエンド: Next.js 16 + TypeScript (strict, no any) + Vanilla CSS（Tailwind 禁止）。
  デザイントークン単一情報源（tokens.css）。DataTable コンポーネント必須。
- バックエンド: FastAPI + Python 3.11（全関数型アノテーション、Pydantic v2 frozen=True、Any 禁止、async/await）。
  モジュール行数は最大 800 行上限。
- データベース: SQLite + Alembic（将来的な PostgreSQL 移行を考慮したスキーマ設計）。
- 非同期 & スケジューリング: Celery + Redis（Windows 環境のため Worker は --pool=solo 必須）。
  ※ Celery Beat は過去タスク一斉多重発火防止のため常時自動起動せず、手動制御とする。
- リアルタイム配信: FastAPI SSE / WebSocket。
================================================================================
```

---

## 3. 意思決定・要件ヒアリング用プロンプト（Phase 0）

Phase 0 において、Claude Code がユーザーに対して要件確認を行うためのヒアリング項目プロンプトです。過去の実績に基づく推奨決定値がセットされています。

```markdown
以下の未決事項および設計パラメータについて確認させてください。各項目には本プロジェクトでの実績に基づく【推奨値】を記載しています。問題なければ推奨値のまま確定します。

1. 【Champion 昇格ゲートの最小ペーパー成績日数】
   - 推奨: 20 営業日（`PAPER_MIN_DAYS=20`）。
   - 理由: 短期的な市場ノイズによる偶発的な好成績を排除し、統計的有意性を担保するため。

2. 【通知チャネルと常駐形態】
   - 推奨: デスクトップ常駐ブラウザ向け Web Push（VAPID）のみ。PWA やモバイルアプリ化は行わない。
   - 理由: トレーディング支援端末としてのプロ仕様・高密度 UI に特化するため。

3. 【LLM プロバイダ構成】
   - 推奨: マルチプロバイダ動的解決（`services/llm/registry.py`）。
     - 公式判定ライン: Anthropic（Claude 3.5 Sonnet）
     - シャドウ並走ライン: Google Gemini（Gemini 2.5 Flash / Pro）または OpenAI（GPT-4o）
   - 理由: LLM ごとの判断の癖をシャドウ比較し、耐障害性と客観性を高めるため。

4. 【Obsidian Vault との連携境界】
   - 推奨: `VAULT_ROOT` を環境変数で指定。`Tickers/*.md` の frontmatter（財務指標）のみ読み取り。
     本文（特色など）はインジェクション防止のため読み飛ばす。書き込みは `Daily/AlphaForge/` 配下に限定。

5. 【四季報データソースの調達方針】
   - 推奨: 当面スタブ（`SHIKIHO_ENABLED=false`）。
   - 理由: 有料 Web サイト等の無断スクレイピングは規約違反および法的リスクがあるため恒久禁止とし、
     将来の正規 API / 法人契約調達時まで Vault frontmatter と J-Quants で代替する。

6. 【UI 配色テーマ】
   - 推奨: "Organic" デザインシステム（背景: 明るいクリーム色 `#f5ead8`、プライマリ: テラコッタオレンジ `#c67139`）。
     ただし、上昇（赤）/ 下落（緑）の国内市場規約はブランドカラーと独立して絶対厳守する。
```

---

## 4. フェーズ別詳細実装プロンプト（Phase 0 〜 Phase 8）

各フェーズの実装を開始する際に、順次 Claude Code へ投入するプロンプトです。

### Phase 0: 調査・計画 & ドキュメンテーション
```markdown
【指示: Phase 0 調査・計画】
リポジトリのルートに `plans/` ディレクトリを作成し、以下のドキュメント一式を Markdown で作成してください。
1. `plans/01_PRD.md`: 製品要求仕様書、ユーザーストーリー、受け入れ基準。
2. `plans/02_アーキテクチャ.md`: コンポーネント構成、データフロー、セキュリティ境界図。
3. `plans/03_システム設計.md`: DB スキーマ（Ledger, Outcomes, Models 等）、API エンドポイント定義、Celery スケジュール。
4. `plans/04_タスクリスト.md`: P1 から P8 までの詳細タスク分割と完了チェックボックス。
5. `plans/05_決定ログと未決事項.md`: 前述のヒアリング結果と確定事項の記録。
作成完了後、内容を提示してユーザーの承認を得てください。
```

### Phase 1: 開発基盤 & ランタイム環境
```markdown
【指示: Phase 1 開発基盤】
リポジトリの骨格を構築し、CI およびローカル開発環境を立ち上げてください。
- バックエンド: FastAPI アプリケーション雛形（`backend/main.py`, `backend/config.py`）、ヘルスチェック `/health`。
- フロントエンド: Next.js 16 + TypeScript プロジェクト（`frontend/`）。Vanilla CSS 構成、`tokens.css` によるデザイントークン定義、アクセシビリティ対応 `DataTable` コンポーネント、免責フッタ。
- DB: SQLite + Alembic 初期マイグレーション（ベースラインテーブル）。
- 非同期: Celery アプリケーション設定（`backend/celery_app.py`）、Redis 接続確認。
- プロセス起動定義: `.claude/launch.json` を作成（frontend: 3001, backend: 8002）。
- CI 設定: GitHub Actions（lint, type-check, test, build）。
完了条件: `npx next build` および `pytest` が警告ゼロで成功すること。
```

### Phase 2: データ取得層 & プロンプト防御境界
```markdown
【指示: Phase 2 データ層 & セキュリティ境界】
市場データおよびメタデータの収集パイプラインを実装してください。
- 株価・指数: J-Quants および yfinance 取得サービス。日経VI 代替としての `^VIX` 取得。
- トレンド取得: ニュース見出しからトピックを構造化する Trend Agent。
- ナレッジ連携: Obsidian Vault の `Tickers/*.md` および `Daily/*.md` から frontmatter のみをパースするパーサー。
- 【セキュリティ境界の実装とテスト】:
  外部由来テキストの本文が LLM プロンプトに絶対に混入しないことを検証する専用テスト（`test_security_boundary.py`）を作成し、強固な防壁を固定してください。
```

### Phase 3: スコアリングエンジン & 3 値売買提案
```markdown
【指示: Phase 3 スコアリング & ピック生成】
銘柄スコアリングおよび売買価格算出ロジックを実装してください。
- 4 分析サブスコア: テクニカル、トレンド、ファンダメンタル、センチメント（各 0〜100 点）。
- 合成スコア: composite_score、方向一致度（concordance）、方向性（direction）。
- 3 値ブラケット算出: ATR や支持抵抗線に基づく 推奨買値 (`entry`)、損切値 (`stop`)、推奨売値 (`target`) の算出。
- サーバ側検証バリデータ: `stop < 現在値 < target` かつ `stop < entry < target` を厳密検証。
- ピック生成 API: `POST /api/picks/run`（中長期 `mid_term` / 短期 `short_term`）。
```

### Phase 4: 予測台帳（Ledger） & 決着評価パイプライン
```markdown
【指示: Phase 4 予測台帳 & 決着】
継続学習の基盤となる予測台帳と決着記録バッチを実装してください。
- 予測台帳（`prediction_ledger` テーブル）: 予測時点の特徴量スナップショット、3 値、確度、スコア内訳、モデルバージョンをイミュータブルに保存。
- 決着バッチ（`services/ledger/resolver.py`）: 短期（1〜3 日）、中長期（5, 20, 60 日）の決着を追跡。MFE/MAE、損切/利確到達順、TOPIX 超過を算出。
- 評価指標算出: Brier スコア、Calibration curve、IC（情報係数）、勝率、損益曲線の集計ロジック。
```

### Phase 5: 継続学習ループ & Champion/Challenger
```markdown
【指示: Phase 5 継続学習ループ】
自律的なモデル改善と検証ゲートを実装してください。
- モデルレジストリ（`model_registry` テーブル）: XGBoost, Random Forest などのバージョン管理。
- 較正エンジン: Isotonic Regression / Platt Scaling による確度較正。
- ウォークフォワード時系列検証: 未来データリークのない時系列 CV。
- Shadow 推論: 本番リクエストに対して Champion と Challenger を並走推論。
- 昇格ゲート: 精度向上かつ較正非悪化かつペーパー成績良好を満たした場合に昇格を「提案」（自動反映はせず人間が承認）。
```

### Phase 6: AI 思考プロセスのリアルタイム可視化
```markdown
【指示: Phase 6 思考可視化】
推論パイプラインの進行状況をライブでストリーミングする機能を実装してください。
- ステージ化パイプライン: 収集 → サブスコア → 合成 → LLM 深掘り → ブラケット → 検証。
- リアルタイム配信: FastAPI SSE / WebSocket エンドポイント。
- フロントエンド UI:
  - パイプライン全体の DAG ビュー（進行状況ステータス表示）。
  - LLM の思考過程がストリーミングで流れる「Thinking パネル」。
  - 過去の推論トレースをリプレイできる履歴再生ビュー。
```

### Phase 7: ポートフォリオ監視 & 承認制通知
```markdown
【指示: Phase 7 ポートフォリオ & 通知】
保有銘柄の監視と Human-in-the-Loop 売買判定を実装してください。
- 保有銘柄登録・管理（数量、取得単価、取得日）。
- 場中監視（平日 9:00〜15:30 JST）: 保有銘柄ごとに「継続/利確/損切/買い増し」を判定。
- 承認キュー UI: AI の判定をチケット化し、ユーザーが承認/却下/約定報告できるワークフロー。
- Web Push 通知: VAPID ベースのブラウザ通知プッシュ。
- EOD レビュー: 大引け後の振り返りと、翌日プロンプトへのヒューリスティクス蓄積。
```

### Phase 8: プロ端末 UI 統合 & モデルラボ
```markdown
【指示: Phase 8 UI 統合 & 仕上げ】
プロ向けトレーディング端末としての洗練された UI と、運用ダッシュボードを完成させてください。
- ダッシュボード: ライブティッカー、ポートフォリオ概況サマリ、本日ピック一覧（中長期/短期）。
- 銘柄詳細画面: チャート（lightweight-charts 等）+ 4 分析レーダー + AI 思考トレース。
- モデルラボ画面: 精度成長曲線、Calibration curve、モデルバージョン比較表、再学習ログ。
- 品質検証: レスポンシブ（640/768/900/1280px）、WCAG 2.2 AA アクセシビリティ、全 CI チェック全通し。
```

---

## 5. 発展的機能拡張プロンプト（運用フェーズの知見）

実際の運用（Phase 9 以降）で導入された、極めて実践的で価値の高い発展機能のプロンプト集です。必要に応じて追加投入してください。

### 5.1 マルチ LLM プロバイダ動的選択 & シャドウ並走
```markdown
【追加指示: マルチ LLM プロバイダ動的解決】
LLM 呼び出し層（`services/llm/registry.py`）を新設し、機能別（`stock_pick`, `portfolio_signal`, `eod_review`, `trend_analyzer`, `note_publish`）に
公式プロバイダ（Anthropic / OpenAI / Gemini）とシャドウプロバイダを環境変数および設定画面から柔軟に切り替えられるようにしてください。
シャドウプロバイダの推論結果は DB に併記保存し、UI 上で公式判定とシャドウ判定（各プロバイダ名バッジ付き）を横並びで比較表示できるようにしてください。
```

### 5.2 Point-in-Time (PIT) ファンダメンタル & センチメントスナップショット
```markdown
【追加指示: PIT スナップショットと未来リーク防止】
過去時点のファンダメンタル指標およびニュースセンチメントを再現可能にするため、日次のスナップショット台帳（`pit_fundamental_snapshots`, `pit_sentiment_snapshots`）を構築してください。
機械学習特徴量パネルを構築する際は、必ず `pandas.merge_asof(direction="backward")` を使用し、
予測日より未来のスナップショットが混入する Lookahead Bias（未来リーク）を構造的に排除してください。
また、十分な日数が蓄積されるまで学習投入をブロックする被覆率ゲート（120 営業日 / 70%）を設けてください。
```

### 5.3 note 下書き自動生成 & プレーンテキスト執筆フロー
```markdown
【追加指示: note 記事執筆ワークフロー】
当日の AI ピックと市場分析を note.com 向けに発信するパイプラインを実装してください。
- 投資助言業抵触を回避するため、note 記事向けプロンプトには売買 3 値（entry/stop/target）を渡さないこと。
- note.com エディタの自動リスト化バグやマークダウン非対応を回避するため、本文はマークダウンではなく【プレーンテキスト（記号や絵文字で見出し表現）】で出力すること。
- 表やレーダー比較は matplotlib で画像化（`docs/note/YYYY-MM-DD/images/`）すること。
- note.com への反映は下書き保存までとし、【公開ボタンは絶対に押さない】こと。
```

### 5.4 日次パイプラインログ自動集約
```markdown
【追加指示: 日次パイプラインログ集約】
毎営業日 17:00 に、候補プール全銘柄のスコア、LLM 見解、3 値、検証ゲート結果、決着、再学習結果を 1 本の Markdown に集約し、
Obsidian Vault 内（`Daily/AlphaForge/<日付>/pipeline_log.md`）へ自動保存する Celery タスクを実装してください。
```

### 5.5 並列学習アーキテクチャ（ProcessPoolExecutor）
```markdown
【追加指示: Celery Solo 下での並列機械学習】
Windows 環境における Celery の安定稼働のため Worker は `--pool=solo` を維持したまま、
複数銘柄のモデル学習や特徴量生成タスク内部で `concurrent.futures.ProcessPoolExecutor` を明示的に起動して並列化してください。
テスト実行時は子プロセスの環境引き継ぎ問題を回避するため、スレッドプールに差し替え可能な間接層（`_create_worker_pool`）を設けてください。
```

---

## 6. 完成受け入れ基準（Definition of Done）

プロジェクトが完成したと判定するための最終チェックリストです。

- [ ] **3 値売買提案**: 全ピックに「推奨買値 / 損切値 / 推奨売値」が付与され、サーバ側で `stop < 現在値 < target` かつ `stop < entry < target` が強制されている。
- [ ] **継続学習ループ**: 全予測が台帳化（`prediction_ledger`）され、複数ホライズン（短期 1〜3日、中長期 5〜60日）で自動決着（`pick_outcomes`）されている。
- [ ] **Champion / Challenger**: 新モデルが Shadow 推論で並走し、ホールドアウト精度・較正・ペーパー成績を満たした場合のみ昇格提案が行われる（承認制）。
- [ ] **事後較正**: 確度が Isotonic / Platt で較正され、UI 上で同バケットの過去実測勝率が確認できる。
- [ ] **AI 思考の可視化**: 収集から検証までのパイプラインがライブストリーミング（SSE/WS）され、リプレイ可能である。
- [ ] **承認制ポートフォリオ**: AI は売買提案チケットを出すのみで、実発注や自動約定は一切行われない。
- [ ] **国内証券標準 UI**: 上昇・利益 = 赤、下落・損失 = 緑。時刻表示はすべて JST。銘柄コードは数字 4 桁。
- [ ] **プロンプトインジェクション多層防御**: Vault、ニュース、四季報等の生本文が LLM プロンプトへ注入されないことがテストで証明されている。
- [ ] **法的コンプライアンス**: 全画面フッタに免責事項が常設され、断定的な投資勧誘文言が存在しない。
- [ ] **Zero Warning CI**: バックエンド（Ruff, Black, Mypy, Bandit, Pytest）およびフロントエンド（TypeScript, ESLint, Jest, Next Build）がすべて警告ゼロで通過する。
- [ ] **日本語規約**: ドキュメント、コード内 Why コメント、コミットメッセージがすべて自然な日本語で記述されている。
