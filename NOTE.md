# 「今日のノート」作成ワークフロー

「今日のノート」「今日のnote」と言われた場合に、Claude Code が実行する記事作成手順。
CLAUDE.md の「Note作成」ワークフロー（`POST /api/notes/generate` を使う自動下書き生成 →
`Daily/AlphaForge/<日付>/` へのエクスポート）とは別系統の、**手動でLLMが本文を執筆する**
ワークフロー。使い分けは以下の通り。

| ワークフロー | 起点 | 出力先 | 本文の書き手 |
|---|---|---|---|
| CLAUDE.md「Note作成」 | `POST /api/notes/generate` | `Daily/AlphaForge/<日付>/`（Vault側） | アプリのnote生成サービス |
| 本ワークフロー（今日のノート） | 本ファイルの手順 | `docs/note/<日付>/`（リポジトリ側） | Claude Codeが対話しながら執筆 |

## 出力先の構成

```
docs/note/YYYY-MM-DD/
  note.txt          ← note.com に貼り付ける本文（プレーンテキスト）
  images/            ← 本文中で参照するスクリーンショット・表画像
    01_xxx.jpg       ← ブラウザ操作で撮ったUIスクリーンショット
    06_xxx_table.png ← matplotlibで描画した表画像
```

## note.txt はマークダウンではなくプレーンテキストで書く

**理由**：note.com のエディタは見出し記号（`#`）・強調記号（`**`）・パイプ表（`| a | b |`）・
画像記法（`![]()`）をそのまま文字として貼り付けてしまい、記法として解釈しない。そのまま
コピペできる状態＝プレーンテキストで用意する必要がある。

- 見出しは `1. 🌍 セクション名` のように番号＋絵文字＋日本語のみ（`#` は使わない）
- 強調したい語句は `【】` や `■` などの全角記号で表現する（`**bold**` は使わない）
- 表は本文中に直接書かず、後述の手順で画像化し、貼り付け場所を
  `［画像挿入位置：説明／ images/ファイル名 ］` の形式で明記する
- 画像埋め込み記法（`![alt](path)`）も使わない。同様に挿入位置マーカーで示す

## 記事の章立て（デフォルト7章）

1. 🌍 今日のグローバルトピックス＆寄り前市況
2. 📈 前日ピックの決着レビュー＆改善点
3. 🤖 AIモデルの思考プロセス＆スクリーニング方針
4. 📊 本日のAIピック一覧（サマリーテーブル）
5. 🔍 各ピック銘柄の徹底解説＆チャート分析
6. ⚖️ 4分析レーダー比較
7. 📝 免責事項

本文冒頭にタイトル案（複数可）とハッシュタグ案も日本語で用意する。

## 実行手順

1. **システム起動確認**：frontend(3001)・backend(8002)・Redis・celery worker・celery beat・
   kb_creator(8077) が動いていなければ起動する（🔧2026-09-22、celery beatも起動対象 — CLAUDE.md参照）。
2. **休場日チェック**：`GET /api/market/snapshot` の `market_status` を確認する。
   「引け後」で現在値がピック上のentry値と完全一致する場合、休場中に前営業日終値が
   引き継がれている可能性が高い。日本の祝日カレンダーと突き合わせて休場日と判断した場合は、
   **記事内で明示的に開示する**（隠して「本日の値動き」であるかのように書かない）。
3. **本日分ピックの有無を確認**：`GET /api/picks/today` 等で確認し、無ければ
   `POST /api/picks/run`（`mid_term`／`short_term` それぞれ）で生成する。
   これは `prediction_ledger` への不可逆な書き込みを伴うため、**必ずユーザーに実行可否を
   確認してから行う**（休場日データに基づく生成であればなおさら明示して確認する）。
4. **前日ピックの決着状況を確認**：`POST /api/ledger/resolve` を実行し、
   `pick_outcomes` の解決件数を確認する。休場が続くと決着が先送りになる点も記事の
   「前日ピックの決着レビュー」に反映する。
5. **ピック詳細の取得**：各ピックを `GET /api/picks/{pick_id}` で取得し、
   `sub_scores`（4分析）・`rationale_text`・`rationale_struct.llm_risk_factors`・
   `rationale_struct.news_sentiment` を本文の思考プロセス・銘柄解説に反映する。
6. **グローバル市況の取得**：WebSearch で前営業日の米国市場・為替・金利トピックを収集する。
7. **スクリーンショット取得**（claude-in-chrome）：
   - ダッシュボード（中長期タブ／短期タブ）
   - 気になる銘柄の詳細パネル（チャート＋4分析内訳）
   - 銘柄詳細画面の「AI推論トレース」（収集→サブスコア→合成→LLM深掘り→ブラケット→検証）
   - チャート画面（任意銘柄）
   撮影したスクリーンショットは `docs/note/<日付>/images/` に整理された連番ファイル名で保存する。
8. **表の画像化**：本日のAIピック一覧・4分析レーダー比較などの表は、matplotlib等で
   画像として描画し `images/06_*.png` のように保存する（日本語フォントは
   `Yu Gothic` / `Meiryo` / `MS Gothic` のいずれか利用可能なものを指定する）。
   本文中は表そのものではなく、挿入位置マーカーのみを書く。
9. **note.txt の執筆**：上記素材をもとに7章構成でプレーンテキストを執筆し、
   `docs/note/<日付>/note.txt` に保存する。タイトル案・ハッシュタグを冒頭に含める。
   **本文中に「AlphaForge」というアプリ名は書かない**（個人的なツールの名称のため）。
   キャプションは「AlphaForge ダッシュボード（〜）」ではなく「ダッシュボード画面（〜）」、
   免責事項の「本アプリケーション（AlphaForge）は〜」は「本ツールは〜」のように、
   アプリ名を出さない一般的な言い回しに置き換える。タイトル・見出し・本文どこにも
   使わない。スクリーンショット画像そのものに写り込むロゴ・見出し文字列（UI上の
   「ALPHA FORGE」表示）は撮影後にトリミングまたはマスクして消す。
10. **note.comへの下書き投稿**：ユーザーの指示があれば、`note-com-draft` スキル
    （`~/.claude/skills/note-com-draft/SKILL.md`）に従ってブラウザ操作で note.com の
    下書き画面を開き `note.txt` の本文と `images/` の画像を貼り付ける。
    **「投稿/公開」ボタンは押さない**。下書き保存の状態で止め、実際の公開はユーザーが
    手動で行う（2026-09-17 決定事項、CLAUDE.md「Note作成」ワークフローと同じ方針）。
    note.comのエディタ固有の落とし穴（見出し行頭の `N. ` がリストに化けて消える問題、
    画像アップロード手順、キャプション入力後の本文フローへの戻し方など）はこのスキル側に
    集約する。本ファイルには重複記載しない。

## 過去の学び

- 2026-09-22：`POST /api/picks/run` に祝日・休場日ガードがないため、休場日でも
  前営業日終値ベースでピックが生成されてしまうことが判明。休場日にAI判断結果を配信する
  場合は、本文中で「休場日であること」「参照している営業日終値の日付」を必ず明記すること。
- 2026-09-22：note.comエディタは段落先頭で `2. ` のようなASCII数字+ピリオド+スペースを
  打つと、空の段落であれば自動的に番号付きリストへ変換してしまい、打った `2. ` 自体が
  消える（可視テキストとして残らない）。詳細な回避策・復旧手順は `note-com-draft` スキル
  参照。
